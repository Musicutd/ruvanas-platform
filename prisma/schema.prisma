generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  SUPER_ADMIN
  SUPPORT
  OWNER
  MANAGER
  CONTENT_EDITOR
  VIEWER
  STUDENT
}

enum EnterpriseIdentityProtocol {
  OIDC
  SAML
}

enum EnterpriseIdentityProviderStatus {
  DRAFT
  READY
  DISABLED
}

enum ServiceAccountStatus {
  ACTIVE
  REVOKED
}

enum ApiKeyStatus {
  ACTIVE
  REVOKED
}

enum IntegrationKind {
  OUTGOING_WEBHOOK
  POS_METRICS
  INVENTORY_METRICS
  FOOTFALL_METRICS
}

enum IntegrationConnectionStatus {
  CONNECTED
  DEGRADED
  DISCONNECTED
  REVOKED
}

enum IntegrationSyncRunStatus {
  PENDING
  RUNNING
  SUCCEEDED
  FAILED
}

enum IntegrationMetricType {
  POS_NET_SALES_MINOR
  POS_TRANSACTION_COUNT
  INVENTORY_UNITS_ON_HAND
  INVENTORY_STOCKOUT_COUNT
  FOOTFALL_ENTRIES
  FOOTFALL_EXITS
}

enum WebhookDeliveryStatus {
  PENDING
  DELIVERED
  FAILED
  ABANDONED
}

enum JobType {
  NOTIFICATION_DELIVERY
}

enum JobStatus {
  QUEUED
  LEASED
  RETRY_SCHEDULED
  SUCCEEDED
  DEAD_LETTER
}

enum OperationalServiceKind {
  WEB
  OPERATIONS_WORKER
  AUDIO_WORKER
}

enum RecoveryAssetKind {
  DATABASE
  OBJECT_STORAGE
}

enum RecoveryEvidenceKind {
  BACKUP_VERIFICATION
  RESTORE_DRILL
}

enum RecoveryEvidenceResult {
  PASSED
  PARTIAL
  FAILED
}

enum NotificationType {
  PLAYER_OFFLINE
  STREAM_ERROR
  CAMPAIGN_FAILURE
  PRODUCTION_ORDER_UPDATE
  BILLING_STATE
  SCHOOL_REVIEW_REQUEST
  CONSENT_EXPIRY
}

enum NotificationSeverity {
  INFO
  WARNING
  CRITICAL
}

enum NotificationChannel {
  IN_APP
  EMAIL
  WEBHOOK
}

enum NotificationDeliveryStatus {
  PENDING
  DELIVERED
  SKIPPED
  FAILED
}

enum AIAssistantType {
  PROMO_SCRIPT
  SCHEDULE_RULES
  ANALYTICS_SUMMARY
  SCHOOL_SCRIPT
  SCHOOL_SHOW_PLAN
  SCHOOL_PRONUNCIATION
}

enum AIJobStatus {
  NEEDS_REVIEW
  APPROVED
  REJECTED
  FAILED
}

enum AIDataClassification {
  INTERNAL
  CUSTOMER_CONTENT
  SCHOOL_CONTENT
  SCHOOL_STUDENT_DATA
}

enum SchoolNetworkRole {
  OWNER
  ADMIN
  VIEWER
}

enum SchoolEpisodeExchangeOfferStatus {
  AVAILABLE
  PAUSED
  WITHDRAWN
}

enum SchoolEpisodeExchangeRequestStatus {
  PENDING
  APPROVED
  DECLINED
  CANCELLED
  REVOKED
}

enum SubscriptionStatus {
  TRIAL
  ACTIVE
  PAST_DUE
  CANCELLED
  SUSPENDED
}

enum ComplimentaryAccessStatus {
  ISSUED
  ACTIVE
  REVOKED
}

enum BillingProvider {
  MANUAL
  GENERIC_HMAC
}

enum BillingInvoiceStatus {
  DRAFT
  OPEN
  PAID
  VOID
  UNCOLLECTIBLE
}

enum BillingWebhookStatus {
  RECEIVED
  PROCESSED
  IGNORED
  FAILED
}

enum BillingReconciliationStatus {
  PENDING
  MATCHED
  MISMATCHED
  RESOLVED
}

enum StationStatus {
  DRAFT
  PENDING_SETUP
  ACTIVE
  PAUSED
  SUSPENDED
  CANCELLED
}

enum LocationStatus {
  DRAFT
  ACTIVE
  PAUSED
  CLOSED
}

enum ZoneStatus {
  ACTIVE
  PAUSED
  OFFLINE
}

enum ChannelStatus {
  DRAFT
  ACTIVE
  PAUSED
  ARCHIVED
}

enum StreamServerType {
  SHOUTCAST_V2
  ICECAST_V2
}

enum AudioCodec {
  MP3
  AAC
}

enum SourceConnectionStatus {
  DISCONNECTED
  CONNECTING
  CONNECTED
  ERROR
}

enum StreamProbeStatus {
  HEALTHY
  DEGRADED
  UNREACHABLE
  SKIPPED
}

enum PlayoutEventType {
  WORKER_STARTED
  WORKER_STOPPED
  WORKER_HEARTBEAT
  SOURCE_CONNECTING
  SOURCE_CONNECTED
  SOURCE_DISCONNECTED
  SOURCE_ERROR
  TRACK_STARTED
  TRACK_FINISHED
  TRACK_FAILED
}

enum MediaType {
  MUSIC
  COMMERCIAL
  JINGLE
  ANNOUNCEMENT
  VOICEOVER
}

enum MediaStatus {
  UPLOADING
  READY
  PROCESSING
  REJECTED
  ARCHIVED
  DELETED
}

enum MediaLibraryType {
  RUVANAS_CATALOGUE
  ORGANISATION_PROMO
}

enum PromoAssetStatus {
  ACTIVE
  ARCHIVED
}

enum PromoVersionStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  REJECTED
  SUPERSEDED
}

enum PromoQcStatus {
  PENDING
  PASSED
  FAILED
}

enum PromoSourceType {
  UPLOAD
  LEGACY_IMPORT
  STUDIO
}

enum PromoProcessingJobType {
  PREVIEW
  TRANSCODE
  LOUDNESS_ANALYSIS
}

enum PromoProcessingJobStatus {
  QUEUED
  RUNNING
  SUCCEEDED
  FAILED
  CANCELLED
}

enum CampaignStatus {
  DRAFT
  PUBLISHED
  PAUSED
  ENDED
  ARCHIVED
}

enum CampaignPriority {
  LOW
  NORMAL
  HIGH
  VERY_HIGH
}

enum CampaignTargetType {
  ALL_LOCATIONS
  BRAND
  LOCATION_GROUP
  LOCATION
  ZONE
}

enum CampaignSchedulingMode {
  PLAYS_PER_HOUR
  INTERVAL
  EXACT_TIMES
  ADVANCED_DAYPART
  SMART_PRIORITY
}

enum CampaignWindowMode {
  PLAYS_PER_HOUR
  INTERVAL
  EXACT_TIME
}

enum RetailMediaPartnerKind {
  ADVERTISER
  AGENCY
}

enum RetailMediaPartnerStatus {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum RetailMediaInventoryStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum RetailMediaPriceModel {
  FIXED_FEE
  PER_PLAY
  CPM
  CUSTOM
}

enum RetailMediaInventoryTargetType {
  BRAND
  LOCATION_GROUP
  ZONE
}

enum RetailMediaOrderStatus {
  DRAFT
  SUBMITTED
  APPROVED
  REJECTED
  CANCELLED
  FULFILLED
}

enum RetailMediaCreativeStatus {
  PENDING
  APPROVED
  REJECTED
}

enum DigitalSignageAssetKind {
  IMAGE
  VIDEO
}

enum DigitalSignageAssetStatus {
  PROCESSING
  READY
  FAILED
  REJECTED
  ARCHIVED
}

enum DigitalSignageVideoJobStatus {
  QUEUED
  RUNNING
  SUCCEEDED
  FAILED
}

enum DigitalSignageTakeoverStatus {
  DRAFT
  ACTIVE
  ENDED
  CANCELLED
}

enum DigitalSignageLayoutStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum DigitalSignagePlaylistStatus {
  DRAFT
  PUBLISHED
  PAUSED
  ARCHIVED
}

enum DigitalSignageDeliveryEventType {
  STARTED
  COMPLETED
  FAILED
}

enum DigitalSignageOrientation {
  LANDSCAPE
  PORTRAIT
  SQUARE
  CUSTOM
}

enum PlayerStatus {
  PENDING_ENROLMENT
  ONLINE
  OFFLINE
  DISABLED
}

enum PlayerHeartbeatSampleKind {
  PERIODIC
  RECOVERY
}

enum PlayerHealthIncidentKind {
  HEARTBEAT_MISSED
  SOURCE_ERROR
  PLAYBACK_ERROR
  MANUAL
}

enum PlayerHealthIncidentSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum PlayerHealthIncidentStatus {
  OPEN
  ACKNOWLEDGED
  RESOLVED
}

enum PlayerCommandKind {
  PING
  REFRESH_STATE
  REFRESH_MANIFEST
  COLLECT_DIAGNOSTICS
}

enum PlayerCommandStatus {
  PENDING
  DELIVERED
  ACKNOWLEDGED
  FAILED
  EXPIRED
  CANCELLED
}

enum TrackStatus {
  DRAFT
  READY
  ARCHIVED
}

enum MusicModeStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum MusicScheduleStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum ProofOfPlayEventType {
  STARTED
  COMPLETED
  FAILED
  INTERRUPTED
}

enum PlaybackItemType {
  MUSIC
  PROMO
  SCHOOL_ANNOUNCEMENT
}

enum SchoolPublishingPolicy {
  PRIVATE
  UNLISTED
  PUBLIC
}

enum SchoolSafeguardingReadinessStatus {
  DRAFT
  READY_FOR_REVIEW
  CHANGES_REQUESTED
  APPROVED
}

enum SchoolSafeguardingReviewDecision {
  CHANGES_REQUESTED
  APPROVED
}

enum SchoolRetentionHoldScope {
  ORGANISATION
  EPISODE
  CONTRIBUTOR
  MEDIA_ASSET
}

enum SchoolPilotReadinessStatus {
  IN_PROGRESS
  BLOCKED
  READY
}

enum SchoolPilotRunStatus {
  PLANNED
  ACTIVE
  PAUSED
  COMPLETED
  CANCELLED
}

enum SchoolPilotEventKind {
  DRILL
  INCIDENT
}

enum SchoolPilotEventCategory {
  EMERGENCY_WITHDRAWAL
  SERVICE_RECOVERY
  SUPPORT_ESCALATION
  CONTENT_SAFETY
  PLATFORM_AVAILABILITY
  OTHER
}

enum SchoolPilotEventSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum SchoolPilotEventStatus {
  OPEN
  ACKNOWLEDGED
  RESOLVED
}

enum SchoolPilotEventOutcome {
  PASSED
  NEEDS_ACTION
  NOT_APPLICABLE
}

enum SchoolConsentModel {
  SCHOOL_POLICY
  PARENT_OR_GUARDIAN
  BOTH
}

enum SchoolStudentIdentityMode {
  DISABLED
  INVITATION_ONLY
  IDENTITY_FEDERATION
}

enum SchoolAnnouncementStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  CHANGES_REQUESTED
  REJECTED
  ARCHIVED
}

enum SchoolNoticeboardStatus {
  SCHEDULED
  CANCELLED
}

enum SchoolNoticeboardTheme {
  INFORMATION
  CELEBRATION
  IMPORTANT
}

enum SchoolBroadcastSlotStatus {
  APPROVED
  CANCELLED
  COMPLETED
}

enum SchoolProgrammeStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum SchoolEpisodeStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  CHANGES_REQUESTED
  REJECTED
  ARCHIVED
}

enum SchoolRundownStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  CHANGES_REQUESTED
  REJECTED
  ARCHIVED
}

enum SchoolRundownItemType {
  MUSIC_TRACK
  JINGLE
  VOICE_TRACK
  INTERVIEW
  ANNOUNCEMENT
  SCRIPT_NOTE
  HARD_TIME
  FLEXIBLE_MARKER
}

enum SchoolPublicationScope {
  INTERNAL_ONLY
  PUBLIC
}

enum StudentContributorStatus {
  ACTIVE
  INACTIVE
}

enum SchoolStudentAccessStatus {
  INVITED
  ACTIVE
  REVOKED
}

enum SchoolPublicationDecisionType {
  PUBLISHED
  UNPUBLISHED
  AUTO_WITHDRAWN
}

enum SchoolSubmissionStatus {
  SUBMITTED
  SUPERSEDED
  WITHDRAWN
}

enum AssignmentStatus {
  DRAFT
  OPEN
  CLOSED
  ARCHIVED
}

enum AssignmentSubmissionStatus {
  SUBMITTED
  REVISION_REQUESTED
  ASSESSED
  WITHDRAWN
}

enum AssessmentStatus {
  DRAFT
  RELEASED
}

enum PortfolioEntryStatus {
  PRIVATE
  ARCHIVED
}

enum SchoolModerationDecision {
  APPROVED
  CHANGES_REQUESTED
  REJECTED
}

enum ConsentRecordStatus {
  PENDING
  GRANTED
  REVOKED
  EXPIRED
}

enum AudioProjectType {
  QUICK_RECORD
  VOICE_TRACK
  MULTITRACK
}

enum AudioProjectStatus {
  DRAFT
  RECORDING
  UPLOADING
  READY
  SUBMITTED
  ARCHIVED
}

enum AudioTakeStatus {
  UPLOADING
  PROCESSING
  READY
  FAILED
  ARCHIVED
}

enum AudioUploadStatus {
  INITIATED
  UPLOADING
  COMPLETING
  COMPLETED
  ABORTED
  FAILED
  EXPIRED
}

enum AudioWaveformStatus {
  PENDING
  RUNNING
  READY
  FAILED
}

enum AudioTrackKind {
  VOICE
  MUSIC
  EFFECT
  MIXED
}

enum AudioClipKind {
  SOURCE
  SILENCE
}

enum AudioMarkerType {
  INTRO
  INTERVIEW
  AD_PSA
  CHAPTER
  EDIT_NOTE
  TEACHER_FEEDBACK
}

enum AudioRenderPreset {
  SCHOOL_RADIO_MP3
  SPEECH_MP3
  WAV_MASTER
}

enum AudioRenderStatus {
  QUEUED
  RUNNING
  SUCCEEDED
  FAILED
  CANCELLED
}

enum SchoolPodcastStatus {
  DRAFT
  PUBLISHED
  UNPUBLISHED
}

enum TranscriptStatus {
  DRAFT
  NEEDS_REVIEW
  APPROVED
}

enum SchoolNewsStoryType {
  NEWS_BULLETIN
  INTERVIEW
  SPORTS_RESULT
  SCHOOL_NOTICE
  FEATURE_STORY
}

enum SchoolNewsStoryStatus {
  PITCH
  ASSIGNED
  SCRIPTING
  FACT_CHECK
  AUDIO_PRODUCTION
  IN_REVIEW
  APPROVED
  PUBLISHED
  ARCHIVED
}

enum LiveStudioStatus {
  CREATED
  SOUNDCHECK
  READY
  ON_AIR
  FALLBACK
  ENDED
}

enum LiveConnectionQuality {
  UNKNOWN
  GOOD
  DEGRADED
  FAILED
}

enum ReportExportStatus {
  QUEUED
  PROCESSING
  READY
  FAILED
  EXPIRED
}

enum DataRequestType {
  EXPORT
  CORRECTION
  DELETION
  RESTRICTION
}

enum DataRequestStatus {
  OPEN
  IN_REVIEW
  AWAITING_INFORMATION
  APPROVED
  COMPLETED
  REJECTED
  CANCELLED
}

enum RetentionJobStatus {
  QUEUED
  DRY_RUN_READY
  COMPLETED
  FAILED
  CANCELLED
}

enum SupportTicketPriority {
  LOW
  NORMAL
  HIGH
  URGENT
}

enum SupportTicketStatus {
  OPEN
  IN_PROGRESS
  WAITING_CUSTOMER
  RESOLVED
  CLOSED
}

enum ProductionOrderStatus {
  DRAFT
  SUBMITTED
  IN_PRODUCTION
  AWAITING_CUSTOMER_APPROVAL
  CHANGES_REQUESTED
  APPROVED
  DELIVERED
  CANCELLED
}

enum ProductionOrderPriority {
  STANDARD
  PRIORITY
  URGENT
}

enum ProductionFundingType {
  PLAN_INCLUDED
  PAID_ADD_ON
}

enum ProductionFundingStatus {
  LEGACY_UNMETERED
  PENDING
  RESERVED
  CONSUMED
  RELEASED
}

enum ProductionCreditEntryType {
  GRANT
  PURCHASE
  RESERVE
  CONSUME
  RELEASE
  EXPIRY
  ADJUSTMENT
}

enum ProductionOrderEventType {
  CREATED
  STATUS_CHANGED
  ASSIGNMENT_CHANGED
  FILE_ADDED
  SCRIPT_VERSION_CREATED
  REVISION_REQUESTED
  REVISION_RESOLVED
  FUNDING_CHANGED
  PROMO_HANDOFF_CREATED
}

enum ProductionOrderFileKind {
  BRIEF_ATTACHMENT
  AUDIO_PREVIEW
  FINAL_MASTER
}

enum ProductionRevisionStatus {
  OPEN
  RESOLVED
}

model User {
  id           String   @id @default(cuid())
  name         String?
  email        String   @unique
  passwordHash String
  role         UserRole @default(OWNER)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  memberships                       OrganisationMember[]
  sentOrganisationInvitations       OrganisationInvitation[]         @relation("OrganisationInvitationInvitedBy")
  acceptedOrganisationInvitations   OrganisationInvitation[]         @relation("OrganisationInvitationAcceptedBy")
  auditLogs                         AuditLog[]                       @relation("AuditActor")
  sessions                          Session[]
  submittedPromoVersions            PromoVersion[]                   @relation("PromoVersionSubmittedBy")
  reviewedPromoVersions             PromoVersion[]                   @relation("PromoVersionReviewedBy")
  publishedCampaigns                Campaign[]                       @relation("CampaignPublishedBy")
  reportExports                     ReportExportJob[]                @relation("ReportExportRequestedBy")
  confirmedTrackRights              Track[]                          @relation("TrackRightsConfirmedBy")
  createdSchoolAnnouncements        SchoolAnnouncement[]             @relation("SchoolAnnouncementCreatedBy")
  reviewedSchoolAnnouncements       SchoolAnnouncement[]             @relation("SchoolAnnouncementReviewedBy")
  createdSchoolNoticeboardPosts     SchoolNoticeboardPost[]          @relation("SchoolNoticeboardCreatedBy")
  cancelledSchoolNoticeboardPosts   SchoolNoticeboardPost[]          @relation("SchoolNoticeboardCancelledBy")
  approvedSchoolSlots               SchoolBroadcastSlot[]            @relation("SchoolBroadcastSlotApprovedBy")
  createdProductionOrders           ProductionOrder[]                @relation("ProductionOrderCreatedBy")
  assignedProductionOrders          ProductionOrder[]                @relation("ProductionOrderAssignedTo")
  productionOrderEvents             ProductionOrderEvent[]           @relation("ProductionOrderEventActor")
  productionOrderFiles              ProductionOrderFile[]            @relation("ProductionOrderFileUploadedBy")
  productionScriptVersions          ProductionScriptVersion[]        @relation("ProductionScriptVersionCreatedBy")
  requestedProductionRevisions      ProductionRevisionRequest[]      @relation("ProductionRevisionRequestedBy")
  resolvedProductionRevisions       ProductionRevisionRequest[]      @relation("ProductionRevisionResolvedBy")
  productionCreditEntries           ProductionCreditLedgerEntry[]    @relation("ProductionCreditEntryActor")
  schoolSupervisions                StaffSupervisor[]                @relation("SchoolSupervisorUser")
  createdStudentGroups              StudentGroup[]                   @relation("StudentGroupCreatedBy")
  createdSchoolProgrammes           SchoolProgramme[]                @relation("SchoolProgrammeCreatedBy")
  createdSchoolEpisodes             SchoolEpisode[]                  @relation("SchoolEpisodeCreatedBy")
  schoolSubmissions                 SchoolSubmission[]               @relation("SchoolSubmissionSubmittedBy")
  schoolModerationReviews           SchoolModerationReview[]         @relation("SchoolModerationReviewer")
  recordedConsentRecords            ConsentRecord[]                  @relation("ConsentRecordedBy")
  createdAudioProjects              AudioProject[]                   @relation("AudioProjectCreatedBy")
  audioProjectVersions              AudioProjectVersion[]            @relation("AudioProjectVersionCreatedBy")
  recordedAudioTakes                AudioTake[]                      @relation("AudioTakeRecordedBy")
  schoolAudioUploads                SchoolAudioUploadSession[]       @relation("SchoolAudioUploadCreatedBy")
  createdAudioMarkers               AudioMarker[]                    @relation("AudioMarkerCreatedBy")
  requestedAudioRenders             AudioRender[]                    @relation("AudioRenderRequestedBy")
  createdSchoolRundowns             SchoolRundown[]                  @relation("SchoolRundownCreatedBy")
  reviewedSchoolRundowns            SchoolRundown[]                  @relation("SchoolRundownReviewedBy")
  createdPodcastSeries              SchoolPodcastSeries[]            @relation("SchoolPodcastSeriesCreatedBy")
  createdPodcastEpisodes            SchoolPodcastEpisode[]           @relation("SchoolPodcastEpisodeCreatedBy")
  reviewedPodcastEpisodes           SchoolPodcastEpisode[]           @relation("SchoolPodcastEpisodeReviewedBy")
  schoolPublicationDecisions        SchoolPublicationDecision[]      @relation("SchoolPublicationDecisionActor")
  createdNewsStories                SchoolNewsStory[]                @relation("SchoolNewsStoryCreatedBy")
  assignedNewsStories               SchoolNewsStory[]                @relation("SchoolNewsStoryAssignedTo")
  reviewedNewsStories               SchoolNewsStory[]                @relation("SchoolNewsStoryReviewedBy")
  supervisedLiveSessions            LiveStudioSession[]              @relation("LiveStudioSupervisor")
  createdAssignments                Assignment[]                     @relation("AssignmentCreatedBy")
  recordedAssignmentSubmissions     AssignmentSubmission[]           @relation("AssignmentSubmissionRecordedBy")
  assignmentAssessments             Assessment[]                     @relation("AssessmentAssessedBy")
  createdPortfolioEntries           PortfolioEntry[]                 @relation("PortfolioEntryCreatedBy")
  createdSchoolNetworks             SchoolNetwork[]                  @relation("SchoolNetworkCreatedBy")
  schoolNetworkMemberships          SchoolNetworkMember[]
  createdSchoolEpisodeExchanges     SchoolEpisodeExchangeOffer[]     @relation("SchoolEpisodeExchangeCreatedBy")
  requestedSchoolEpisodeExchanges   SchoolEpisodeExchangeRequest[]   @relation("SchoolEpisodeExchangeRequestedBy")
  decidedSchoolEpisodeExchanges     SchoolEpisodeExchangeRequest[]   @relation("SchoolEpisodeExchangeDecidedBy")
  importedSchoolEpisodeExchanges    SchoolEpisodeExchangeRequest[]   @relation("SchoolEpisodeExchangeImportedBy")
  schoolSafeguardingReviews         SchoolSafeguardingReview[]       @relation("SchoolSafeguardingReviewReviewer")
  createdSchoolRetentionHolds       SchoolRetentionHold[]            @relation("SchoolRetentionHoldCreatedBy")
  releasedSchoolRetentionHolds      SchoolRetentionHold[]            @relation("SchoolRetentionHoldReleasedBy")
  updatedSchoolPilotReadiness       SchoolPilotReadiness[]           @relation("SchoolPilotReadinessUpdatedBy")
  createdSchoolPilotRuns            SchoolPilotRun[]                 @relation("SchoolPilotRunCreatedBy")
  updatedSchoolPilotRuns            SchoolPilotRun[]                 @relation("SchoolPilotRunUpdatedBy")
  createdSchoolPilotEvents          SchoolPilotEvent[]               @relation("SchoolPilotEventCreatedBy")
  acknowledgedSchoolPilotEvents     SchoolPilotEvent[]               @relation("SchoolPilotEventAcknowledgedBy")
  resolvedSchoolPilotEvents         SchoolPilotEvent[]               @relation("SchoolPilotEventResolvedBy")
  schoolStudentAccess               SchoolStudentAccess?             @relation("SchoolStudentAccessUser")
  invitedSchoolStudentAccess        SchoolStudentAccess[]            @relation("SchoolStudentAccessInvitedBy")
  enterpriseIdentityLinks           EnterpriseIdentityLink[]
  createdServiceAccounts            ServiceAccount[]                 @relation("ServiceAccountCreatedBy")
  createdCompliancePolicies         CompliancePolicy[]               @relation("CompliancePolicyCreatedBy")
  policyAcceptances                 PolicyAcceptance[]               @relation("PolicyAcceptanceAcceptedBy")
  requestedDataRequests             DataRequest[]                    @relation("DataRequestRequestedBy")
  subjectDataRequests               DataRequest[]                    @relation("DataRequestSubjectUser")
  completedDataRequests             DataRequest[]                    @relation("DataRequestCompletedBy")
  reviewedRetentionPolicies         RetentionPolicy[]                @relation("RetentionPolicyReviewedBy")
  requestedRetentionJobs            RetentionJob[]                   @relation("RetentionJobRequestedBy")
  createdSupportTickets             SupportTicket[]                  @relation("SupportTicketCreatedBy")
  assignedSupportTickets            SupportTicket[]                  @relation("SupportTicketAssignedTo")
  requestedAIJobs                   AIJob[]                          @relation("AIJobRequestedBy")
  reviewedAIJobs                    AIJob[]                          @relation("AIJobReviewedBy")
  recommendationFeedback            RecommendationFeedback[]
  createdIntegrationConnections     IntegrationConnection[]          @relation("IntegrationConnectionCreatedBy")
  createdRetailMediaInventory       RetailMediaInventoryPackage[]    @relation("RetailMediaInventoryCreatedBy")
  approvedRetailMediaOrders         RetailMediaOrder[]               @relation("RetailMediaOrderApprovedBy")
  fulfilledRetailMediaOrders        RetailMediaOrder[]               @relation("RetailMediaOrderFulfilledBy")
  reviewedRetailMediaCreatives      RetailMediaOrderCreative[]       @relation("RetailMediaCreativeReviewedBy")
  reviewedRetailMediaVisuals        RetailMediaOrderVisualCreative[] @relation("RetailMediaVisualCreativeReviewedBy")
  uploadedDigitalSignageAssets      DigitalSignageAsset[]            @relation("DigitalSignageAssetUploadedBy")
  createdDigitalSignageLayouts      DigitalSignageLayout[]           @relation("DigitalSignageLayoutCreatedBy")
  createdDigitalSignageDevices      DigitalSignageDevice[]           @relation("DigitalSignageDeviceCreatedBy")
  createdDigitalSignagePlaylists    DigitalSignagePlaylist[]         @relation("DigitalSignagePlaylistCreatedBy")
  publishedDigitalSignagePlaylists  DigitalSignagePlaylist[]         @relation("DigitalSignagePlaylistPublishedBy")
  createdDigitalSignageTakeovers    DigitalSignageTakeover[]         @relation("DigitalSignageTakeoverCreatedBy")
  activatedDigitalSignageTakeovers  DigitalSignageTakeover[]         @relation("DigitalSignageTakeoverActivatedBy")
  endedDigitalSignageTakeovers      DigitalSignageTakeover[]         @relation("DigitalSignageTakeoverEndedBy")
  acknowledgedPlayerHealthIncidents PlayerHealthIncident[]           @relation("PlayerHealthIncidentAcknowledgedBy")
  resolvedPlayerHealthIncidents     PlayerHealthIncident[]           @relation("PlayerHealthIncidentResolvedBy")
  acknowledgedStreamHealthIncidents StationStreamHealthIncident[]    @relation("StationStreamHealthIncidentAcknowledgedBy")
  resolvedStreamHealthIncidents     StationStreamHealthIncident[]    @relation("StationStreamHealthIncidentResolvedBy")
  requestedPlayerCommands           PlayerCommand[]                  @relation("PlayerCommandRequestedBy")
  notificationPreferences           NotificationPreference[]
  notificationDeliveries            NotificationDelivery[]
  updatedRecoveryControls           RecoveryControl[]                @relation("RecoveryControlUpdatedBy")
  recordedRecoveryEvidence          RecoveryEvidence[]               @relation("RecoveryEvidenceRecordedBy")
  createdComplimentaryAccessCodes   ComplimentaryAccessCode[]        @relation("ComplimentaryAccessCreatedBy")
  redeemedComplimentaryAccessCodes  ComplimentaryAccessCode[]        @relation("ComplimentaryAccessRedeemedBy")
  revokedComplimentaryAccessCodes   ComplimentaryAccessCode[]        @relation("ComplimentaryAccessRevokedBy")
}

model Organisation {
  id        String   @id @default(cuid())
  name      String
  slug      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  members                          OrganisationMember[]
  invitations                      OrganisationInvitation[]
  subscription                     Subscription?
  stations                         Station[]
  brands                           Brand[]
  locations                        Location[]
  locationGroups                   LocationGroup[]
  channels                         Channel[]
  mediaAssets                      MediaAsset[]                      @relation("OrganisationMediaAssets")
  promoAssets                      PromoAsset[]
  auditLogs                        AuditLog[]
  players                          Player[]
  musicModes                       MusicMode[]
  musicSchedules                   MusicSchedule[]
  proofOfPlayEvents                ProofOfPlayEvent[]
  campaigns                        Campaign[]
  playoutIntents                   PlayoutIntent[]
  reportExports                    ReportExportJob[]
  analyticsHourlyAggregates        AnalyticsHourlyAggregate[]
  analyticsAggregationCursor       AnalyticsAggregationCursor?
  activeSessions                   Session[]
  schoolProfile                    SchoolProfile?
  schoolSafeguardingReadiness      SchoolSafeguardingReadiness?
  schoolSafeguardingReviews        SchoolSafeguardingReview[]
  schoolRetentionHolds             SchoolRetentionHold[]
  schoolPilotReadiness             SchoolPilotReadiness?
  schoolPilotRuns                  SchoolPilotRun[]
  schoolPilotEvents                SchoolPilotEvent[]
  schoolAnnouncements              SchoolAnnouncement[]
  schoolNoticeboardPosts           SchoolNoticeboardPost[]
  schoolBroadcastSlots             SchoolBroadcastSlot[]
  productionOrders                 ProductionOrder[]
  productionOrderEvents            ProductionOrderEvent[]
  productionOrderFiles             ProductionOrderFile[]
  productionScriptVersions         ProductionScriptVersion[]
  productionRevisionRequests       ProductionRevisionRequest[]
  productionCreditEntries          ProductionCreditLedgerEntry[]
  staffSupervisors                 StaffSupervisor[]
  studentGroups                    StudentGroup[]
  studentContributors              StudentContributor[]
  schoolStudentAccess              SchoolStudentAccess[]
  schoolProgrammes                 SchoolProgramme[]
  schoolEpisodes                   SchoolEpisode[]
  schoolSubmissions                SchoolSubmission[]
  schoolModerationReviews          SchoolModerationReview[]
  consentRecords                   ConsentRecord[]
  audioProjects                    AudioProject[]
  audioTakes                       AudioTake[]
  schoolAudioUploads               SchoolAudioUploadSession[]
  audioRenders                     AudioRender[]
  schoolRundowns                   SchoolRundown[]
  schoolPodcastSeries              SchoolPodcastSeries[]
  schoolPodcastEpisodes            SchoolPodcastEpisode[]
  schoolPublicationDecisions       SchoolPublicationDecision[]
  schoolPublicationDailyAggregates SchoolPublicationDailyAggregate[]
  transcripts                      Transcript[]
  schoolNewsStories                SchoolNewsStory[]
  liveStudioSessions               LiveStudioSession[]
  assignments                      Assignment[]
  assignmentSubmissions            AssignmentSubmission[]
  billingAccount                   BillingAccount?
  billingInvoices                  BillingInvoice[]
  billingReconciliations           BillingUsageReconciliation[]
  assessments                      Assessment[]
  portfolioEntries                 PortfolioEntry[]
  schoolNetworkSchool              SchoolNetworkSchool?
  sourceSchoolEpisodeExchanges     SchoolEpisodeExchangeOffer[]      @relation("SchoolEpisodeExchangeSource")
  targetSchoolEpisodeRequests      SchoolEpisodeExchangeRequest[]    @relation("SchoolEpisodeExchangeTarget")
  enterpriseSecurityPolicy         EnterpriseSecurityPolicy?
  enterpriseIdentityProviders      EnterpriseIdentityProvider[]
  serviceAccounts                  ServiceAccount[]
  policyAcceptances                PolicyAcceptance[]
  dataRequests                     DataRequest[]
  retentionPolicy                  RetentionPolicy?
  retentionJobs                    RetentionJob[]
  supportTickets                   SupportTicket[]
  auditExportSeals                 AuditExportSeal[]
  aiJobs                           AIJob[]
  integrationConnections           IntegrationConnection[]
  integrationSyncRuns              IntegrationSyncRun[]
  integrationMetricSummaries       IntegrationMetricSummary[]
  outgoingWebhookEvents            OutgoingWebhookEvent[]
  playerHeartbeatSamples           PlayerHeartbeatSample[]
  playerHealthIncidents            PlayerHealthIncident[]
  playerCommands                   PlayerCommand[]
  playerListenerLeases             PlayerListenerLease[]
  streamHealthSamples              StationStreamHealthSample[]
  streamHealthIncidents            StationStreamHealthIncident[]
  jobs                             Job[]
  notificationEvents               NotificationEvent[]
  notificationPreferences          NotificationPreference[]
  notificationDeliveries           NotificationDelivery[]
  retailMediaPartners              RetailMediaPartner[]
  retailMediaInventory             RetailMediaInventoryPackage[]
  retailMediaOrders                RetailMediaOrder[]
  digitalSignageAssets             DigitalSignageAsset[]
  digitalSignageLayouts            DigitalSignageLayout[]
  digitalSignageDevices            DigitalSignageDevice[]
  digitalSignagePlaylists          DigitalSignagePlaylist[]
  digitalSignageDeliveryProofs     DigitalSignageDeliveryProof[]
  digitalSignageTakeovers          DigitalSignageTakeover[]
  complimentaryAccessCodes         ComplimentaryAccessCode[]
}

model OrganisationMember {
  id             String   @id @default(cuid())
  userId         String
  organisationId String
  role           UserRole @default(OWNER)
  createdAt      DateTime @default(now())

  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([userId, organisationId])
  @@index([organisationId])
}

model OrganisationInvitation {
  id               String   @id @default(cuid())
  organisationId   String
  email            String
  role             UserRole
  tokenHash        String?  @unique
  expiresAt        DateTime
  acceptedAt       DateTime?
  revokedAt        DateTime?
  invitedByUserId  String
  acceptedByUserId String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  invitedBy    User         @relation("OrganisationInvitationInvitedBy", fields: [invitedByUserId], references: [id], onDelete: Restrict)
  acceptedBy   User?        @relation("OrganisationInvitationAcceptedBy", fields: [acceptedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, email])
  @@index([organisationId, acceptedAt, revokedAt, expiresAt])
  @@index([invitedByUserId])
}

model SchoolNetwork {
  id              String   @id @default(cuid())
  name            String
  slug            String   @unique
  createdByUserId String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  createdBy             User                         @relation("SchoolNetworkCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  members               SchoolNetworkMember[]
  schools               SchoolNetworkSchool[]
  episodeExchangeOffers SchoolEpisodeExchangeOffer[]
  auditLogs             AuditLog[]

  @@index([createdByUserId])
}

model SchoolNetworkMember {
  id              String            @id @default(cuid())
  schoolNetworkId String
  userId          String
  role            SchoolNetworkRole @default(VIEWER)
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt

  schoolNetwork SchoolNetwork @relation(fields: [schoolNetworkId], references: [id], onDelete: Cascade)
  user          User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([schoolNetworkId, userId])
  @@index([userId])
  @@index([schoolNetworkId, role])
}

model SchoolNetworkSchool {
  id              String   @id @default(cuid())
  schoolNetworkId String
  organisationId  String   @unique
  active          Boolean  @default(true)
  joinedAt        DateTime @default(now())
  updatedAt       DateTime @updatedAt

  schoolNetwork SchoolNetwork @relation(fields: [schoolNetworkId], references: [id], onDelete: Cascade)
  organisation  Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([schoolNetworkId, organisationId])
  @@index([schoolNetworkId, active])
}

model SchoolEpisodeExchangeOffer {
  id                     String                           @id @default(cuid())
  schoolNetworkId        String
  sourceOrganisationId   String
  episodeId              String
  approvedPromoVersionId String
  sourceTitle            String
  sourceSummary          String?
  languageCode           String                           @default("und")
  durationSeconds        Int?
  status                 SchoolEpisodeExchangeOfferStatus @default(AVAILABLE)
  consentConfirmed       Boolean
  policyVersion          String                           @default("school-network-exchange-v1")
  createdByUserId        String
  availableAt            DateTime                         @default(now())
  withdrawnAt            DateTime?
  createdAt              DateTime                         @default(now())
  updatedAt              DateTime                         @updatedAt

  schoolNetwork        SchoolNetwork                  @relation(fields: [schoolNetworkId], references: [id], onDelete: Cascade)
  sourceOrganisation   Organisation                   @relation("SchoolEpisodeExchangeSource", fields: [sourceOrganisationId], references: [id], onDelete: Cascade)
  episode              SchoolEpisode                  @relation(fields: [episodeId], references: [id], onDelete: Restrict)
  approvedPromoVersion PromoVersion                   @relation(fields: [approvedPromoVersionId], references: [id], onDelete: Restrict)
  createdBy            User                           @relation("SchoolEpisodeExchangeCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  requests             SchoolEpisodeExchangeRequest[]

  @@unique([schoolNetworkId, episodeId])
  @@index([schoolNetworkId, status, availableAt])
  @@index([sourceOrganisationId, status])
  @@index([approvedPromoVersionId])
  @@index([createdByUserId])
}

model SchoolEpisodeExchangeRequest {
  id                   String                             @id @default(cuid())
  offerId              String
  targetOrganisationId String
  status               SchoolEpisodeExchangeRequestStatus @default(PENDING)
  intendedUse          String
  decisionNotes        String?
  policyVersion        String                             @default("school-network-exchange-v1")
  requestedByUserId    String
  decidedByUserId      String?
  importedByUserId     String?
  requestedAt          DateTime                           @default(now())
  decidedAt            DateTime?
  revokedAt            DateTime?
  importedAt           DateTime?
  createdAt            DateTime                           @default(now())
  updatedAt            DateTime                           @updatedAt

  offer                SchoolEpisodeExchangeOffer @relation(fields: [offerId], references: [id], onDelete: Cascade)
  targetOrganisation   Organisation               @relation("SchoolEpisodeExchangeTarget", fields: [targetOrganisationId], references: [id], onDelete: Cascade)
  requestedBy          User                       @relation("SchoolEpisodeExchangeRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)
  decidedBy            User?                      @relation("SchoolEpisodeExchangeDecidedBy", fields: [decidedByUserId], references: [id], onDelete: SetNull)
  importedBy           User?                      @relation("SchoolEpisodeExchangeImportedBy", fields: [importedByUserId], references: [id], onDelete: SetNull)
  importedAnnouncement SchoolAnnouncement?

  @@unique([offerId, targetOrganisationId])
  @@index([targetOrganisationId, status, requestedAt])
  @@index([requestedByUserId])
  @@index([decidedByUserId])
  @@index([importedByUserId])
}

model Plan {
  id                            String   @id @default(cuid())
  name                          String   @unique
  code                          String   @unique
  monthlyPriceCents             Int
  stationLimit                  Int      @default(1)
  storageLimitGb                Int
  listenerLimit                 Int
  maxBitrateKbps                Int
  includesRuvanasCatalogue      Boolean  @default(false)
  promoUploadEnabled            Boolean  @default(true)
  schoolRadioEnabled            Boolean  @default(false)
  schoolPublicPublishingEnabled Boolean  @default(false)
  retailMediaEnabled            Boolean  @default(false)
  digitalSignageEnabled         Boolean  @default(false)
  active                        Boolean  @default(true)
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt

  subscriptions            Subscription[]
  complimentaryAccessCodes ComplimentaryAccessCode[]
}

model Subscription {
  id                                         String             @id @default(cuid())
  organisationId                             String             @unique
  planId                                     String
  status                                     SubscriptionStatus @default(TRIAL)
  currentPeriodEnd                           DateTime?
  schoolRadioEnabled                         Boolean?
  schoolPublicPublishingEnabled              Boolean?
  retailMediaEnabled                         Boolean?
  digitalSignageEnabled                      Boolean?
  complimentaryAccessCodeId                  String?            @unique
  complimentaryAccessActive                  Boolean            @default(false)
  complimentaryAccessActivatedAt             DateTime?
  complimentaryPlanName                      String?
  complimentaryPlanCode                      String?
  complimentaryStationLimit                  Int?
  complimentaryStorageLimitGb                Int?
  complimentaryListenerLimit                 Int?
  complimentaryMaxBitrateKbps                Int?
  complimentaryIncludesCatalogue             Boolean?
  complimentaryPromoUploadEnabled            Boolean?
  complimentarySchoolRadioEnabled            Boolean?
  complimentarySchoolPublicPublishingEnabled Boolean?
  complimentaryRetailMediaEnabled            Boolean?
  complimentaryDigitalSignageEnabled         Boolean?
  createdAt                                  DateTime           @default(now())
  updatedAt                                  DateTime           @updatedAt

  organisation            Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  plan                    Plan                         @relation(fields: [planId], references: [id])
  billingContract         BillingContract?
  billingInvoices         BillingInvoice[]
  billingReconciliations  BillingUsageReconciliation[]
  complimentaryAccessCode ComplimentaryAccessCode?     @relation("ActiveComplimentaryAccess", fields: [complimentaryAccessCodeId], references: [id], onDelete: SetNull)
}

model ComplimentaryAccessCode {
  id               String                    @id @default(cuid())
  codeHash         String                    @unique
  codeSuffix       String
  organisationId   String
  planId           String
  status           ComplimentaryAccessStatus @default(ISSUED)
  note             String?
  createdByUserId  String
  redeemedByUserId String?
  revokedByUserId  String?
  redeemedAt       DateTime?
  revokedAt        DateTime?
  createdAt        DateTime                  @default(now())
  updatedAt        DateTime                  @updatedAt

  organisation       Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  plan               Plan          @relation(fields: [planId], references: [id], onDelete: Restrict)
  createdBy          User          @relation("ComplimentaryAccessCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  redeemedBy         User?         @relation("ComplimentaryAccessRedeemedBy", fields: [redeemedByUserId], references: [id], onDelete: SetNull)
  revokedBy          User?         @relation("ComplimentaryAccessRevokedBy", fields: [revokedByUserId], references: [id], onDelete: SetNull)
  activeSubscription Subscription? @relation("ActiveComplimentaryAccess")

  @@index([organisationId, status, createdAt])
  @@index([planId, status])
}

model BillingAccount {
  id                 String          @id @default(cuid())
  organisationId     String          @unique
  provider           BillingProvider @default(MANUAL)
  externalCustomerId String?
  active             Boolean         @default(true)
  createdAt          DateTime        @default(now())
  updatedAt          DateTime        @updatedAt

  organisation    Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  contract        BillingContract?
  invoices        BillingInvoice[]
  reconciliations BillingUsageReconciliation[]

  @@unique([provider, externalCustomerId])
  @@index([provider, active])
}

model BillingContract {
  id                     String    @id @default(cuid())
  billingAccountId       String    @unique
  subscriptionId         String    @unique
  externalSubscriptionId String?
  providerStatus         String?
  currentPeriodStart     DateTime?
  currentPeriodEnd       DateTime?
  graceEndsAt            DateTime?
  cancelAtPeriodEnd      Boolean   @default(false)
  lastReconciledAt       DateTime?
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt

  billingAccount BillingAccount @relation(fields: [billingAccountId], references: [id], onDelete: Cascade)
  subscription   Subscription   @relation(fields: [subscriptionId], references: [id], onDelete: Cascade)

  @@unique([billingAccountId, externalSubscriptionId])
  @@index([providerStatus])
  @@index([graceEndsAt])
}

model BillingInvoice {
  id                String               @id @default(cuid())
  organisationId    String
  billingAccountId  String
  subscriptionId    String?
  externalInvoiceId String?
  status            BillingInvoiceStatus @default(DRAFT)
  currency          String               @default("EUR")
  amountDueCents    Int                  @default(0)
  amountPaidCents   Int                  @default(0)
  periodStart       DateTime?
  periodEnd         DateTime?
  dueAt             DateTime?
  paidAt            DateTime?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  organisation    Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  billingAccount  BillingAccount               @relation(fields: [billingAccountId], references: [id], onDelete: Cascade)
  subscription    Subscription?                @relation(fields: [subscriptionId], references: [id], onDelete: SetNull)
  reconciliations BillingUsageReconciliation[]

  @@unique([billingAccountId, externalInvoiceId])
  @@index([organisationId, status])
  @@index([subscriptionId])
  @@index([periodStart, periodEnd])
}

model BillingWebhookEvent {
  id                String               @id @default(cuid())
  provider          BillingProvider
  externalEventId   String
  eventType         String
  payloadSha256     String
  signatureVerified Boolean              @default(false)
  status            BillingWebhookStatus @default(RECEIVED)
  errorMessage      String?
  receivedAt        DateTime             @default(now())
  processedAt       DateTime?

  @@unique([provider, externalEventId])
  @@index([status, receivedAt])
}

model BillingUsageReconciliation {
  id                 String                      @id @default(cuid())
  organisationId     String
  subscriptionId     String
  billingAccountId   String?
  invoiceId          String?
  periodStart        DateTime
  periodEnd          DateTime
  locationCount      Int
  zoneCount          Int
  stationCount       Int
  storageBytes       BigInt
  schoolRadioEnabled Boolean
  providerUsage      Json?
  discrepancies      Json?
  status             BillingReconciliationStatus @default(PENDING)
  resolvedAt         DateTime?
  createdAt          DateTime                    @default(now())
  updatedAt          DateTime                    @updatedAt

  organisation   Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  subscription   Subscription    @relation(fields: [subscriptionId], references: [id], onDelete: Cascade)
  billingAccount BillingAccount? @relation(fields: [billingAccountId], references: [id], onDelete: SetNull)
  invoice        BillingInvoice? @relation(fields: [invoiceId], references: [id], onDelete: SetNull)

  @@unique([organisationId, periodStart, periodEnd])
  @@index([status, createdAt])
  @@index([subscriptionId])
  @@index([billingAccountId])
  @@index([invoiceId])
}

model Brand {
  id             String   @id @default(cuid())
  organisationId String
  name           String
  slug           String
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organisation                Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  locations                   Location[]
  channels                    Channel[]
  campaignTargets             CampaignTarget[]
  retailMediaInventoryTargets RetailMediaInventoryTarget[]

  @@unique([organisationId, slug])
  @@index([organisationId])
}

model Location {
  id             String         @id @default(cuid())
  organisationId String
  brandId        String?
  name           String
  slug           String
  status         LocationStatus @default(DRAFT)
  timezone       String         @default("Europe/Malta")
  addressLine1   String?
  addressLine2   String?
  city           String?
  region         String?
  postalCode     String?
  countryCode    String?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  organisation               Organisation               @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  brand                      Brand?                     @relation(fields: [brandId], references: [id], onDelete: SetNull)
  zones                      Zone[]
  groupMemberships           LocationGroupMember[]
  openingHours               LocationOpeningHour[]
  openingExceptions          LocationOpeningException[]
  musicSchedules             MusicSchedule[]
  campaignTargets            CampaignTarget[]
  schoolBroadcastSlots       SchoolBroadcastSlot[]
  schoolNoticeboardPosts     SchoolNoticeboardPost[]
  integrationMetricSummaries IntegrationMetricSummary[]

  @@unique([organisationId, slug])
  @@index([organisationId])
  @@index([brandId])
}

model LocationOpeningHour {
  id             String   @id @default(cuid())
  locationId     String
  weekday        Int
  isClosed       Boolean  @default(false)
  opensAtMinute  Int?
  closesAtMinute Int?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  location Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([locationId, weekday])
  @@index([locationId])
}

model LocationOpeningException {
  id             String   @id @default(cuid())
  locationId     String
  date           DateTime @db.Date
  isClosed       Boolean  @default(true)
  opensAtMinute  Int?
  closesAtMinute Int?
  label          String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  location Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([locationId, date])
  @@index([locationId, date])
}

model LocationGroup {
  id             String   @id @default(cuid())
  organisationId String
  name           String
  slug           String
  description    String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organisation                Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  locations                   LocationGroupMember[]
  campaignTargets             CampaignTarget[]
  retailMediaInventoryTargets RetailMediaInventoryTarget[]

  @@unique([organisationId, slug])
  @@index([organisationId])
}

model LocationGroupMember {
  locationGroupId String
  locationId      String
  createdAt       DateTime @default(now())

  locationGroup LocationGroup @relation(fields: [locationGroupId], references: [id], onDelete: Cascade)
  location      Location      @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@id([locationGroupId, locationId])
  @@index([locationId])
}

model Zone {
  id         String     @id @default(cuid())
  locationId String
  name       String
  slug       String
  status     ZoneStatus @default(ACTIVE)
  createdAt  DateTime   @default(now())
  updatedAt  DateTime   @updatedAt

  location                    Location                     @relation(fields: [locationId], references: [id], onDelete: Cascade)
  channelAssignments          ChannelAssignment[]
  players                     Player[]
  musicSchedules              MusicSchedule[]
  proofOfPlayEvents           ProofOfPlayEvent[]
  campaignTargets             CampaignTarget[]
  playoutIntents              PlayoutIntent[]
  schoolBroadcastSlots        SchoolBroadcastSlot[]
  schoolNoticeboardPosts      SchoolNoticeboardPost[]
  retailMediaInventoryTargets RetailMediaInventoryTarget[]
  digitalSignageDevices       DigitalSignageDevice[]

  @@unique([locationId, slug])
  @@index([locationId])
}

model Channel {
  id             String        @id @default(cuid())
  organisationId String
  brandId        String?
  stationId      String?
  name           String
  slug           String
  description    String?
  status         ChannelStatus @default(DRAFT)
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  organisation       Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  brand              Brand?              @relation(fields: [brandId], references: [id], onDelete: SetNull)
  station            Station?            @relation(fields: [stationId], references: [id], onDelete: SetNull)
  zoneAssignments    ChannelAssignment[]
  playoutEvents      PlayoutEvent[]
  playoutIntents     PlayoutIntent[]
  proofOfPlayEvents  ProofOfPlayEvent[]
  liveStudioSessions LiveStudioSession[]

  @@unique([organisationId, slug])
  @@index([organisationId])
  @@index([brandId])
  @@index([stationId])
}

model ChannelAssignment {
  id         String    @id @default(cuid())
  channelId  String
  zoneId     String
  activeFrom DateTime  @default(now())
  activeTo   DateTime?
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  channel Channel @relation(fields: [channelId], references: [id], onDelete: Cascade)
  zone    Zone    @relation(fields: [zoneId], references: [id], onDelete: Cascade)

  @@unique([channelId, zoneId])
  @@index([zoneId])
}

model Station {
  id                String        @id @default(cuid())
  organisationId    String
  name              String
  slug              String        @unique
  description       String?
  logoUrl           String?
  status            StationStatus @default(DRAFT)
  storageUsedMb     Int           @default(0)
  listenerLimit     Int
  storageLimitGb    Int
  maxBitrateKbps    Int
  providerName      String?
  providerAccountId String?
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt

  organisation          Organisation                  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  streamConfig          StationStreamConfig?
  channels              Channel[]
  playoutEvents         PlayoutEvent[]
  streamHealthSamples   StationStreamHealthSample[]
  streamHealthIncidents StationStreamHealthIncident[]

  @@index([organisationId])
}

model StationStreamConfig {
  id        String @id @default(cuid())
  stationId String @unique

  centovaUsername String?
  streamUrl       String?
  mountPoint      String?
  serverHost      String?
  serverPort      Int?
  bitrateKbps     Int?

  serverType     StreamServerType @default(SHOUTCAST_V2)
  outputCodec    AudioCodec       @default(MP3)
  sampleRateHz   Int              @default(44100)
  outputChannels Int              @default(2)

  sourceConnectionStatus SourceConnectionStatus @default(DISCONNECTED)
  lastConnectedAt        DateTime?
  lastHeartbeatAt        DateTime?
  lastError              String?

  providerKey           String    @default("CENTOVA_CAST")
  backupStreamUrl       String?
  probeEnabled          Boolean   @default(true)
  probeIntervalSeconds  Int       @default(60)
  probeTimeoutMs        Int       @default(8000)
  lastProbeAt           DateTime?
  lastSuccessfulProbeAt DateTime?
  consecutiveFailures   Int       @default(0)
  lastProbeHttpStatus   Int?
  lastProbeLatencyMs    Int?
  lastProbeContentType  String?

  adminPasswordEncrypted  String?
  sourcePasswordEncrypted String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  station       Station                     @relation(fields: [stationId], references: [id], onDelete: Cascade)
  healthSamples StationStreamHealthSample[]

  @@index([probeEnabled, lastProbeAt])
}

model StationStreamHealthSample {
  id             String            @id @default(cuid())
  organisationId String
  stationId      String
  streamConfigId String
  bucketStart    DateTime
  observedAt     DateTime
  status         StreamProbeStatus
  latencyMs      Int?
  httpStatus     Int?
  contentType    String?
  errorCode      String?
  createdAt      DateTime          @default(now())

  organisation Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  station      Station             @relation(fields: [stationId], references: [id], onDelete: Cascade)
  streamConfig StationStreamConfig @relation(fields: [streamConfigId], references: [id], onDelete: Cascade)

  @@unique([streamConfigId, bucketStart])
  @@index([organisationId, observedAt])
  @@index([stationId, observedAt])
  @@index([status, observedAt])
}

model StationStreamHealthIncident {
  id                  String                       @id @default(cuid())
  organisationId      String
  stationId           String
  status              PlayerHealthIncidentStatus   @default(OPEN)
  severity            PlayerHealthIncidentSeverity @default(MEDIUM)
  firstObservedAt     DateTime
  lastObservedAt      DateTime
  summary             String
  details             Json?
  acknowledgedAt      DateTime?
  acknowledgedById    String?
  acknowledgementNote String?
  resolvedAt          DateTime?
  resolvedById        String?
  resolutionNote      String?
  createdAt           DateTime                     @default(now())
  updatedAt           DateTime                     @updatedAt

  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  station        Station      @relation(fields: [stationId], references: [id], onDelete: Cascade)
  acknowledgedBy User?        @relation("StationStreamHealthIncidentAcknowledgedBy", fields: [acknowledgedById], references: [id], onDelete: SetNull)
  resolvedBy     User?        @relation("StationStreamHealthIncidentResolvedBy", fields: [resolvedById], references: [id], onDelete: SetNull)

  @@index([organisationId, status, severity])
  @@index([stationId, status])
  @@index([lastObservedAt])
}

model PlayoutEvent {
  id         String           @id @default(cuid())
  channelId  String
  stationId  String
  eventType  PlayoutEventType
  assetId    String?
  message    String?
  metadata   Json?
  occurredAt DateTime         @default(now())

  channel Channel @relation(fields: [channelId], references: [id], onDelete: Cascade)
  station Station @relation(fields: [stationId], references: [id], onDelete: Cascade)

  @@index([channelId, occurredAt])
  @@index([stationId, occurredAt])
  @@index([eventType, occurredAt])
}

model MediaAsset {
  id              String           @id @default(cuid())
  organisationId  String?
  libraryType     MediaLibraryType @default(ORGANISATION_PROMO)
  name            String
  originalName    String
  storageKey      String           @unique
  mimeType        String
  sizeBytes       BigInt
  durationSeconds Int?
  mediaType       MediaType
  status          MediaStatus      @default(UPLOADING)
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  organisation       Organisation?       @relation("OrganisationMediaAssets", fields: [organisationId], references: [id], onDelete: Cascade)
  genres             MediaAssetGenre[]
  track              Track?
  promoVersions      PromoVersion[]
  audioTakes         AudioTake[]
  audioClips         AudioClip[]
  audioRenderOutputs AudioRender[]       @relation("AudioRenderOutput")
  proofOfPlayEvents  ProofOfPlayEvent[]
  playoutIntents     PlayoutIntent[]
  schoolRundownItems SchoolRundownItem[]
  transcripts        Transcript[]
  interviewStories   SchoolNewsStory[]   @relation("SchoolNewsInterviewAsset")
  liveRecordings     LiveStudioSession[] @relation("LiveStudioRecordingAsset")

  @@index([organisationId])
  @@index([libraryType])
  @@index([organisationId, libraryType])
  @@index([organisationId, mediaType])
  @@index([organisationId, status])
  @@index([libraryType, status])
}

model PromoAsset {
  id                       String           @id @default(cuid())
  organisationId           String
  name                     String
  mediaType                MediaType
  languageCode             String           @default("und")
  status                   PromoAssetStatus @default(ACTIVE)
  currentApprovedVersionId String?          @unique
  createdAt                DateTime         @default(now())
  updatedAt                DateTime         @updatedAt

  organisation           Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  versions               PromoVersion[]   @relation("PromoAssetVersions")
  currentApprovedVersion PromoVersion?    @relation("CurrentApprovedPromoVersion", fields: [currentApprovedVersionId], references: [id], onDelete: SetNull)
  productionOrder        ProductionOrder? @relation("ProductionOrderPromoAsset")

  @@index([organisationId, status])
  @@index([organisationId, name])
}

model PromoVersion {
  id              String             @id @default(cuid())
  promoAssetId    String
  mediaAssetId    String
  version         Int
  status          PromoVersionStatus @default(DRAFT)
  qcStatus        PromoQcStatus      @default(PENDING)
  sourceType      PromoSourceType    @default(UPLOAD)
  sourceReference String?
  languageCode    String             @default("und")
  checksumSha256  String?
  loudnessLufs    Decimal?           @db.Decimal(5, 2)
  durationSeconds Int?
  qcNotes         String?
  submittedById   String?
  reviewedById    String?
  submittedAt     DateTime?
  reviewedAt      DateTime?
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt

  promoAsset                  PromoAsset                   @relation("PromoAssetVersions", fields: [promoAssetId], references: [id], onDelete: Cascade)
  mediaAsset                  MediaAsset                   @relation(fields: [mediaAssetId], references: [id], onDelete: Restrict)
  submittedBy                 User?                        @relation("PromoVersionSubmittedBy", fields: [submittedById], references: [id], onDelete: SetNull)
  reviewedBy                  User?                        @relation("PromoVersionReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)
  liveFallbackSessions        LiveStudioSession[]
  approvedForPromo            PromoAsset?                  @relation("CurrentApprovedPromoVersion")
  processingJobs              PromoProcessingJob[]
  campaigns                   Campaign[]
  schoolAnnouncements         SchoolAnnouncement[]
  schoolSubmissions           SchoolSubmission[]
  audioTakes                  AudioTake[]
  renderedAudioVersions       AudioRender[]                @relation("AudioRenderPromoVersion")
  playoutIntents              PlayoutIntent[]
  proofOfPlayEvents           ProofOfPlayEvent[]
  schoolRundownItems          SchoolRundownItem[]
  retailMediaCreatives        RetailMediaOrderCreative[]
  schoolEpisodeExchangeOffers SchoolEpisodeExchangeOffer[]

  @@unique([promoAssetId, version])
  @@unique([promoAssetId, sourceReference])
  @@index([promoAssetId, status])
  @@index([mediaAssetId])
  @@index([status, qcStatus])
  @@index([submittedById])
  @@index([reviewedById])
}

model PromoProcessingJob {
  id             String                   @id @default(cuid())
  promoVersionId String
  jobType        PromoProcessingJobType
  status         PromoProcessingJobStatus @default(QUEUED)
  attempts       Int                      @default(0)
  errorMessage   String?
  startedAt      DateTime?
  completedAt    DateTime?
  createdAt      DateTime                 @default(now())
  updatedAt      DateTime                 @updatedAt

  promoVersion PromoVersion @relation(fields: [promoVersionId], references: [id], onDelete: Cascade)

  @@unique([promoVersionId, jobType])
  @@index([status, createdAt])
}

model Campaign {
  id                         String                 @id @default(cuid())
  organisationId             String
  promoVersionId             String
  name                       String
  status                     CampaignStatus         @default(DRAFT)
  priority                   CampaignPriority       @default(NORMAL)
  schedulingMode             CampaignSchedulingMode
  mandatory                  Boolean                @default(false)
  respectOpeningHours        Boolean                @default(true)
  effectiveFrom              DateTime               @db.Date
  effectiveTo                DateTime               @db.Date
  maxPromoMinutesPerHour     Int                    @default(12)
  minSamePromoGapMinutes     Int                    @default(15)
  minAnyPromoGapMinutes      Int                    @default(2)
  publicationRevision        Int                    @default(0)
  publishedConfigurationHash String?
  publishedById              String?
  publishedAt                DateTime?
  createdAt                  DateTime               @default(now())
  updatedAt                  DateTime               @updatedAt

  organisation      Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  promoVersion      PromoVersion       @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  publishedBy       User?              @relation("CampaignPublishedBy", fields: [publishedById], references: [id], onDelete: SetNull)
  targets           CampaignTarget[]
  rule              CampaignRule?
  schedules         CampaignSchedule[]
  playoutIntents    PlayoutIntent[]
  proofOfPlayEvents ProofOfPlayEvent[]
  retailMediaOrder  RetailMediaOrder?

  @@index([organisationId, status])
  @@index([promoVersionId, status])
  @@index([effectiveFrom, effectiveTo])
}

model CampaignTarget {
  id              String             @id @default(cuid())
  campaignId      String
  targetType      CampaignTargetType
  brandId         String?
  locationGroupId String?
  locationId      String?
  zoneId          String?
  createdAt       DateTime           @default(now())

  campaign      Campaign       @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  brand         Brand?         @relation(fields: [brandId], references: [id], onDelete: Cascade)
  locationGroup LocationGroup? @relation(fields: [locationGroupId], references: [id], onDelete: Cascade)
  location      Location?      @relation(fields: [locationId], references: [id], onDelete: Cascade)
  zone          Zone?          @relation(fields: [zoneId], references: [id], onDelete: Cascade)

  @@unique([campaignId, targetType, brandId, locationGroupId, locationId, zoneId])
  @@index([campaignId, targetType])
  @@index([brandId])
  @@index([locationGroupId])
  @@index([locationId])
  @@index([zoneId])
}

model CampaignRule {
  id                 String   @id @default(cuid())
  campaignId         String   @unique
  playsPerHour       Int?
  intervalMinutes    Int?
  exactTimeHardStart Boolean  @default(false)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  campaign Campaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)
}

model CampaignSchedule {
  id              String             @id @default(cuid())
  campaignId      String
  weekday         Int
  windowMode      CampaignWindowMode
  startMinute     Int?
  endMinute       Int?
  exactMinute     Int?
  playsPerHour    Int?
  intervalMinutes Int?
  createdAt       DateTime           @default(now())

  campaign Campaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId, weekday, startMinute])
  @@index([campaignId, weekday, exactMinute])
}

model RetailMediaPartner {
  id               String                   @id @default(cuid())
  organisationId   String
  kind             RetailMediaPartnerKind
  name             String
  legalName        String?
  contactName      String?
  contactEmail     String?
  contactPhone     String?
  billingReference String?
  status           RetailMediaPartnerStatus @default(ACTIVE)
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt

  organisation     Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  advertiserOrders RetailMediaOrder[] @relation("RetailMediaOrderAdvertiser")
  agencyOrders     RetailMediaOrder[] @relation("RetailMediaOrderAgency")

  @@unique([organisationId, kind, name])
  @@index([organisationId, status, kind])
}

model RetailMediaInventoryPackage {
  id               String                     @id @default(cuid())
  organisationId   String
  name             String
  description      String?
  status           RetailMediaInventoryStatus @default(DRAFT)
  priceModel       RetailMediaPriceModel
  currencyCode     String?                    @db.VarChar(3)
  unitPriceMinor   Int?
  maxPlays         Int
  effectiveFrom    DateTime                   @db.Date
  effectiveTo      DateTime                   @db.Date
  restrictionNotes String?
  createdByUserId  String
  createdAt        DateTime                   @default(now())
  updatedAt        DateTime                   @updatedAt

  organisation Organisation                  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy    User                          @relation("RetailMediaInventoryCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  targets      RetailMediaInventoryTarget[]
  dayparts     RetailMediaInventoryDaypart[]
  orders       RetailMediaOrder[]

  @@unique([organisationId, name])
  @@index([organisationId, status, effectiveFrom, effectiveTo])
  @@index([createdByUserId])
}

model RetailMediaInventoryTarget {
  id                 String                         @id @default(cuid())
  inventoryPackageId String
  targetType         RetailMediaInventoryTargetType
  brandId            String?
  locationGroupId    String?
  zoneId             String?
  createdAt          DateTime                       @default(now())

  inventoryPackage RetailMediaInventoryPackage @relation(fields: [inventoryPackageId], references: [id], onDelete: Cascade)
  brand            Brand?                      @relation(fields: [brandId], references: [id], onDelete: Cascade)
  locationGroup    LocationGroup?              @relation(fields: [locationGroupId], references: [id], onDelete: Cascade)
  zone             Zone?                       @relation(fields: [zoneId], references: [id], onDelete: Cascade)

  @@unique([inventoryPackageId, targetType, brandId, locationGroupId, zoneId])
  @@index([inventoryPackageId, targetType])
  @@index([brandId])
  @@index([locationGroupId])
  @@index([zoneId])
}

model RetailMediaInventoryDaypart {
  id                 String   @id @default(cuid())
  inventoryPackageId String
  weekday            Int
  startMinute        Int
  endMinute          Int
  createdAt          DateTime @default(now())

  inventoryPackage RetailMediaInventoryPackage @relation(fields: [inventoryPackageId], references: [id], onDelete: Cascade)

  @@unique([inventoryPackageId, weekday, startMinute, endMinute], map: "RetailMediaDaypart_package_window_key")
  @@index([inventoryPackageId, weekday, startMinute], map: "RetailMediaDaypart_package_weekday_idx")
}

model RetailMediaOrder {
  id                          String                 @id @default(cuid())
  organisationId              String
  advertiserId                String
  agencyId                    String?
  inventoryPackageId          String
  campaignId                  String?                @unique
  name                        String
  purchaseOrderReference      String?
  status                      RetailMediaOrderStatus @default(DRAFT)
  submittedAt                 DateTime?
  approvedByUserId            String?
  approvedAt                  DateTime?
  decisionNote                String?
  fulfilledByUserId           String?
  fulfilledAt                 DateTime?
  fulfilmentRevision          Int                    @default(0)
  fulfilmentConfigurationHash String?
  createdAt                   DateTime               @default(now())
  updatedAt                   DateTime               @updatedAt

  organisation         Organisation                     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  advertiser           RetailMediaPartner               @relation("RetailMediaOrderAdvertiser", fields: [advertiserId], references: [id], onDelete: Restrict)
  agency               RetailMediaPartner?              @relation("RetailMediaOrderAgency", fields: [agencyId], references: [id], onDelete: Restrict)
  inventoryPackage     RetailMediaInventoryPackage      @relation(fields: [inventoryPackageId], references: [id], onDelete: Restrict)
  campaign             Campaign?                        @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  approvedBy           User?                            @relation("RetailMediaOrderApprovedBy", fields: [approvedByUserId], references: [id], onDelete: SetNull)
  fulfilledBy          User?                            @relation("RetailMediaOrderFulfilledBy", fields: [fulfilledByUserId], references: [id], onDelete: SetNull)
  creatives            RetailMediaOrderCreative[]
  visualCreatives      RetailMediaOrderVisualCreative[]
  visualPlaylists      DigitalSignagePlaylist[]
  visualDeliveryProofs DigitalSignageDeliveryProof[]

  @@index([organisationId, status, createdAt])
  @@index([advertiserId])
  @@index([agencyId])
  @@index([inventoryPackageId])
  @@index([approvedByUserId])
  @@index([fulfilledByUserId])
}

model RetailMediaOrderCreative {
  id             String                    @id @default(cuid())
  orderId        String
  promoVersionId String
  status         RetailMediaCreativeStatus @default(PENDING)
  reviewedById   String?
  reviewedAt     DateTime?
  reviewNote     String?
  createdAt      DateTime                  @default(now())
  updatedAt      DateTime                  @updatedAt

  order        RetailMediaOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  promoVersion PromoVersion     @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  reviewedBy   User?            @relation("RetailMediaCreativeReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@unique([orderId, promoVersionId])
  @@index([promoVersionId, status])
  @@index([reviewedById])
}

model RetailMediaOrderVisualCreative {
  id             String                    @id @default(cuid())
  orderId        String
  signageAssetId String
  status         RetailMediaCreativeStatus @default(PENDING)
  reviewedById   String?
  reviewedAt     DateTime?
  reviewNote     String?
  createdAt      DateTime                  @default(now())
  updatedAt      DateTime                  @updatedAt

  order        RetailMediaOrder    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  signageAsset DigitalSignageAsset @relation(fields: [signageAssetId], references: [id], onDelete: Restrict)
  reviewedBy   User?               @relation("RetailMediaVisualCreativeReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@unique([orderId, signageAssetId])
  @@index([signageAssetId, status])
  @@index([reviewedById])
}

model DigitalSignageAsset {
  id               String                    @id @default(cuid())
  organisationId   String
  name             String
  kind             DigitalSignageAssetKind   @default(IMAGE)
  status           DigitalSignageAssetStatus @default(READY)
  originalName     String
  storageKey       String                    @unique
  mimeType         String
  sizeBytes        BigInt
  checksumSha256   String
  width            Int
  height           Int
  durationSeconds  Int?
  uploadedByUserId String
  createdAt        DateTime                  @default(now())
  updatedAt        DateTime                  @updatedAt

  organisation         Organisation                     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  uploadedBy           User                             @relation("DigitalSignageAssetUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: Restrict)
  playlistItems        DigitalSignagePlaylistItem[]
  deliveryProofs       DigitalSignageDeliveryProof[]
  videoJob             DigitalSignageVideoJob?
  retailMediaCreatives RetailMediaOrderVisualCreative[]

  @@unique([organisationId, checksumSha256])
  @@index([organisationId, status, kind])
  @@index([uploadedByUserId])
}

model DigitalSignageVideoJob {
  id             String                       @id @default(cuid())
  assetId        String                       @unique
  status         DigitalSignageVideoJobStatus @default(QUEUED)
  attempts       Int                          @default(0)
  startedAt      DateTime?
  completedAt    DateTime?
  errorMessage   String?
  sourceMimeType String
  createdAt      DateTime                     @default(now())
  updatedAt      DateTime                     @updatedAt

  asset DigitalSignageAsset @relation(fields: [assetId], references: [id], onDelete: Cascade)

  @@index([status, createdAt])
}

model DigitalSignageLayout {
  id              String                     @id @default(cuid())
  organisationId  String
  name            String
  description     String?
  status          DigitalSignageLayoutStatus @default(DRAFT)
  orientation     DigitalSignageOrientation  @default(LANDSCAPE)
  canvasWidth     Int
  canvasHeight    Int
  backgroundColor String                     @default("#000000") @db.VarChar(7)
  createdByUserId String
  createdAt       DateTime                   @default(now())
  updatedAt       DateTime                   @updatedAt

  organisation Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy    User                         @relation("DigitalSignageLayoutCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  regions      DigitalSignageLayoutRegion[]
  playlists    DigitalSignagePlaylist[]

  @@unique([organisationId, name])
  @@index([organisationId, status])
  @@index([createdByUserId])
}

model DigitalSignageLayoutRegion {
  id        String   @id @default(cuid())
  layoutId  String
  name      String
  x         Int
  y         Int
  width     Int
  height    Int
  zIndex    Int      @default(0)
  fitMode   String   @default("COVER") @db.VarChar(12)
  createdAt DateTime @default(now())

  layout        DigitalSignageLayout         @relation(fields: [layoutId], references: [id], onDelete: Cascade)
  playlistItems DigitalSignagePlaylistItem[]

  @@unique([layoutId, name])
  @@index([layoutId, zIndex])
}

model DigitalSignageDevice {
  id                 String                    @id @default(cuid())
  organisationId     String
  zoneId             String
  name               String
  status             PlayerStatus              @default(PENDING_ENROLMENT)
  orientation        DigitalSignageOrientation @default(LANDSCAPE)
  viewportWidth      Int
  viewportHeight     Int
  enrolmentTokenHash String?                   @unique
  enrolmentExpiresAt DateTime?
  sessionTokenHash   String?                   @unique
  enrolledAt         DateTime?
  lastHeartbeatAt    DateTime?
  lastIpAddress      String?
  lastUserAgent      String?
  createdByUserId    String
  createdAt          DateTime                  @default(now())
  updatedAt          DateTime                  @updatedAt

  organisation        Organisation                   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  zone                Zone                           @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  createdBy           User                           @relation("DigitalSignageDeviceCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  playlistAssignments DigitalSignagePlaylistDevice[]
  deliveryProofs      DigitalSignageDeliveryProof[]
  takeoverAssignments DigitalSignageTakeoverDevice[]

  @@index([organisationId, status])
  @@index([zoneId])
  @@index([createdByUserId])
  @@index([lastHeartbeatAt])
}

model DigitalSignagePlaylist {
  id                 String                       @id @default(cuid())
  organisationId     String
  layoutId           String
  name               String
  status             DigitalSignagePlaylistStatus @default(DRAFT)
  version            Int                          @default(1)
  startsAt           DateTime?
  endsAt             DateTime?
  activeDays         Int[]                        @default([])
  dailyStartMinute   Int                          @default(0)
  dailyEndMinute     Int                          @default(1440)
  priority           Int                          @default(0)
  publishedAt        DateTime?
  publishedByUserId  String?
  createdByUserId    String
  retailMediaOrderId String?
  createdAt          DateTime                     @default(now())
  updatedAt          DateTime                     @updatedAt

  organisation     Organisation                   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  layout           DigitalSignageLayout           @relation(fields: [layoutId], references: [id], onDelete: Restrict)
  createdBy        User                           @relation("DigitalSignagePlaylistCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  publishedBy      User?                          @relation("DigitalSignagePlaylistPublishedBy", fields: [publishedByUserId], references: [id], onDelete: SetNull)
  items            DigitalSignagePlaylistItem[]
  devices          DigitalSignagePlaylistDevice[]
  deliveryProofs   DigitalSignageDeliveryProof[]
  retailMediaOrder RetailMediaOrder?              @relation(fields: [retailMediaOrderId], references: [id], onDelete: SetNull)
  takeovers        DigitalSignageTakeover[]

  @@unique([organisationId, name])
  @@index([organisationId, status, startsAt, endsAt])
  @@index([layoutId])
  @@index([createdByUserId])
  @@index([publishedByUserId])
  @@index([retailMediaOrderId])
}

model DigitalSignagePlaylistItem {
  id              String   @id @default(cuid())
  playlistId      String
  regionId        String
  assetId         String
  position        Int
  durationSeconds Int      @default(10)
  createdAt       DateTime @default(now())

  playlist       DigitalSignagePlaylist        @relation(fields: [playlistId], references: [id], onDelete: Cascade)
  region         DigitalSignageLayoutRegion    @relation(fields: [regionId], references: [id], onDelete: Restrict)
  asset          DigitalSignageAsset           @relation(fields: [assetId], references: [id], onDelete: Restrict)
  deliveryProofs DigitalSignageDeliveryProof[]

  @@unique([playlistId, regionId, position])
  @@index([assetId])
  @@index([regionId])
}

model DigitalSignagePlaylistDevice {
  playlistId String
  deviceId   String
  assignedAt DateTime @default(now())

  playlist DigitalSignagePlaylist @relation(fields: [playlistId], references: [id], onDelete: Cascade)
  device   DigitalSignageDevice   @relation(fields: [deviceId], references: [id], onDelete: Cascade)

  @@id([playlistId, deviceId])
  @@index([deviceId])
}

model DigitalSignageTakeover {
  id                String                       @id @default(cuid())
  organisationId    String
  playlistId        String
  name              String
  reason            String
  status            DigitalSignageTakeoverStatus @default(DRAFT)
  startsAt          DateTime
  endsAt            DateTime
  activatedAt       DateTime?
  endedAt           DateTime?
  createdByUserId   String
  activatedByUserId String?
  endedByUserId     String?
  createdAt         DateTime                     @default(now())
  updatedAt         DateTime                     @updatedAt

  organisation   Organisation                   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  playlist       DigitalSignagePlaylist         @relation(fields: [playlistId], references: [id], onDelete: Restrict)
  createdBy      User                           @relation("DigitalSignageTakeoverCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  activatedBy    User?                          @relation("DigitalSignageTakeoverActivatedBy", fields: [activatedByUserId], references: [id], onDelete: SetNull)
  endedBy        User?                          @relation("DigitalSignageTakeoverEndedBy", fields: [endedByUserId], references: [id], onDelete: SetNull)
  devices        DigitalSignageTakeoverDevice[]
  deliveryProofs DigitalSignageDeliveryProof[]

  @@index([organisationId, status, startsAt, endsAt])
  @@index([playlistId])
  @@index([createdByUserId])
}

model DigitalSignageTakeoverDevice {
  takeoverId String
  deviceId   String
  assignedAt DateTime @default(now())

  takeover DigitalSignageTakeover @relation(fields: [takeoverId], references: [id], onDelete: Cascade)
  device   DigitalSignageDevice   @relation(fields: [deviceId], references: [id], onDelete: Cascade)

  @@id([takeoverId, deviceId])
  @@index([deviceId])
}

model DigitalSignageDeliveryProof {
  id                 String                          @id @default(cuid())
  clientEventId      String                          @unique
  organisationId     String
  deviceId           String
  playlistId         String
  playlistItemId     String
  assetId            String
  manifestVersion    String                          @db.VarChar(24)
  eventType          DigitalSignageDeliveryEventType
  occurredAt         DateTime
  receivedAt         DateTime                        @default(now())
  failureReason      String?
  takeoverId         String?
  retailMediaOrderId String?

  organisation     Organisation               @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  device           DigitalSignageDevice       @relation(fields: [deviceId], references: [id], onDelete: Cascade)
  playlist         DigitalSignagePlaylist     @relation(fields: [playlistId], references: [id], onDelete: Restrict)
  playlistItem     DigitalSignagePlaylistItem @relation(fields: [playlistItemId], references: [id], onDelete: Restrict)
  asset            DigitalSignageAsset        @relation(fields: [assetId], references: [id], onDelete: Restrict)
  takeover         DigitalSignageTakeover?    @relation(fields: [takeoverId], references: [id], onDelete: SetNull)
  retailMediaOrder RetailMediaOrder?          @relation(fields: [retailMediaOrderId], references: [id], onDelete: SetNull)

  @@index([organisationId, occurredAt])
  @@index([deviceId, occurredAt])
  @@index([playlistId, eventType, occurredAt])
  @@index([assetId, occurredAt])
  @@index([takeoverId, occurredAt])
  @@index([retailMediaOrderId, occurredAt])
}

model Track {
  id                   String      @id @default(cuid())
  mediaAssetId         String      @unique
  title                String
  artist               String
  album                String?
  releaseYear          Int?
  isExplicit           Boolean     @default(false)
  status               TrackStatus @default(DRAFT)
  rightsHolder         String?
  rightsReference      String?
  permittedTerritories String?
  licenceExpiresAt     DateTime?   @db.Date
  rightsConfirmedAt    DateTime?
  rightsConfirmedById  String?
  createdAt            DateTime    @default(now())
  updatedAt            DateTime    @updatedAt

  mediaAsset         MediaAsset          @relation(fields: [mediaAssetId], references: [id], onDelete: Cascade)
  rightsConfirmedBy  User?               @relation("TrackRightsConfirmedBy", fields: [rightsConfirmedById], references: [id], onDelete: SetNull)
  musicModes         MusicModeTrack[]
  proofOfPlayEvents  ProofOfPlayEvent[]
  schoolRundownItems SchoolRundownItem[]

  @@index([status])
  @@index([artist, title])
  @@index([rightsConfirmedById])
  @@index([licenceExpiresAt])
}

model MusicMode {
  id             String          @id @default(cuid())
  organisationId String
  name           String
  slug           String
  description    String?
  status         MusicModeStatus @default(DRAFT)
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  organisation  Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  tracks        MusicModeTrack[]
  scheduleSlots ScheduleSlot[]

  @@unique([organisationId, slug])
  @@index([organisationId, status])
}

model MusicModeTrack {
  musicModeId String
  trackId     String
  weight      Int      @default(100)
  createdAt   DateTime @default(now())

  musicMode MusicMode @relation(fields: [musicModeId], references: [id], onDelete: Cascade)
  track     Track     @relation(fields: [trackId], references: [id], onDelete: Cascade)

  @@id([musicModeId, trackId])
  @@index([trackId])
}

model SchoolProfile {
  id               String                 @id @default(cuid())
  organisationId   String                 @unique
  displayName      String?
  publishingPolicy SchoolPublishingPolicy @default(PRIVATE)
  policyVersion    String                 @default("school-radio-v1")
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([publishingPolicy])
}

model SchoolSafeguardingReadiness {
  id                           String                            @id @default(cuid())
  organisationId               String                            @unique
  status                       SchoolSafeguardingReadinessStatus @default(DRAFT)
  targetCountries              Json                              @default("[]")
  minimumStudentAge            Int?
  maximumStudentAge            Int?
  consentModel                 SchoolConsentModel?
  studentIdentityMode          SchoolStudentIdentityMode         @default(DISABLED)
  privacyContactEmail          String?
  rawRecordingRetentionDays    Int?
  consentEvidenceRetentionDays Int?
  localPolicyReference         String?
  notes                        String?
  staffModerationConfirmed     Boolean                           @default(false)
  noDirectMessagingConfirmed   Boolean                           @default(false)
  privateByDefaultConfirmed    Boolean                           @default(false)
  submittedAt                  DateTime?
  submittedByUserId            String?
  createdAt                    DateTime                          @default(now())
  updatedAt                    DateTime                          @updatedAt

  organisation Organisation               @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  reviews      SchoolSafeguardingReview[]

  @@index([status])
}

model SchoolSafeguardingReview {
  id             String                           @id @default(cuid())
  readinessId    String
  organisationId String
  reviewerUserId String
  decision       SchoolSafeguardingReviewDecision
  notes          String?
  policyVersion  String                           @default("school-safeguarding-readiness-v1")
  policySnapshot Json
  createdAt      DateTime                         @default(now())

  readiness    SchoolSafeguardingReadiness @relation(fields: [readinessId], references: [id], onDelete: Cascade)
  organisation Organisation                @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  reviewer     User                        @relation("SchoolSafeguardingReviewReviewer", fields: [reviewerUserId], references: [id], onDelete: Restrict)

  @@index([readinessId, createdAt])
  @@index([organisationId, createdAt])
  @@index([reviewerUserId])
}

model SchoolRetentionHold {
  id               String                   @id @default(cuid())
  organisationId   String
  scope            SchoolRetentionHoldScope
  referenceId      String?
  reason           String
  createdByUserId  String
  releasedByUserId String?
  releaseReason    String?
  releasedAt       DateTime?
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy    User         @relation("SchoolRetentionHoldCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  releasedBy   User?        @relation("SchoolRetentionHoldReleasedBy", fields: [releasedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, releasedAt, createdAt])
  @@index([organisationId, scope, referenceId])
  @@index([createdByUserId])
  @@index([releasedByUserId])
}

model SchoolPilotReadiness {
  id                                String                     @id @default(cuid())
  organisationId                    String                     @unique
  status                            SchoolPilotReadinessStatus @default(IN_PROGRESS)
  staffTrainingConfirmed            Boolean                    @default(false)
  emergencyWithdrawalDrillConfirmed Boolean                    @default(false)
  retentionReviewConfirmed          Boolean                    @default(false)
  supportContactsConfirmed          Boolean                    @default(false)
  recoveryPlanConfirmed             Boolean                    @default(false)
  notes                             String?
  updatedByUserId                   String
  readyAt                           DateTime?
  createdAt                         DateTime                   @default(now())
  updatedAt                         DateTime                   @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  updatedBy    User         @relation("SchoolPilotReadinessUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: Restrict)

  @@index([status, updatedAt])
  @@index([updatedByUserId])
}

model SchoolPilotRun {
  id                String               @id @default(cuid())
  organisationId    String
  title             String
  status            SchoolPilotRunStatus @default(PLANNED)
  plannedStartAt    DateTime
  plannedEndAt      DateTime
  startedAt         DateTime?
  endedAt           DateTime?
  notes             String?
  transitionReason  String?
  readinessSnapshot Json                 @default("{}")
  createdByUserId   String
  updatedByUserId   String
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  organisation Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy    User               @relation("SchoolPilotRunCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  updatedBy    User               @relation("SchoolPilotRunUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: Restrict)
  events       SchoolPilotEvent[]

  @@index([organisationId, status, plannedStartAt])
  @@index([createdByUserId])
  @@index([updatedByUserId])
}

model SchoolPilotEvent {
  id                   String                   @id @default(cuid())
  organisationId       String
  pilotRunId           String
  kind                 SchoolPilotEventKind
  category             SchoolPilotEventCategory
  severity             SchoolPilotEventSeverity
  status               SchoolPilotEventStatus   @default(OPEN)
  outcome              SchoolPilotEventOutcome?
  summary              String
  responseActions      String?
  resolutionNotes      String?
  occurredAt           DateTime
  acknowledgedAt       DateTime?
  acknowledgedByUserId String?
  resolvedAt           DateTime?
  resolvedByUserId     String?
  createdByUserId      String
  createdAt            DateTime                 @default(now())
  updatedAt            DateTime                 @updatedAt

  organisation   Organisation   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  pilotRun       SchoolPilotRun @relation(fields: [pilotRunId], references: [id], onDelete: Cascade)
  createdBy      User           @relation("SchoolPilotEventCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  acknowledgedBy User?          @relation("SchoolPilotEventAcknowledgedBy", fields: [acknowledgedByUserId], references: [id], onDelete: SetNull)
  resolvedBy     User?          @relation("SchoolPilotEventResolvedBy", fields: [resolvedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, occurredAt])
  @@index([pilotRunId, occurredAt])
  @@index([createdByUserId])
  @@index([acknowledgedByUserId])
  @@index([resolvedByUserId])
}

model SchoolAnnouncement {
  id                      String                   @id @default(cuid())
  organisationId          String
  promoVersionId          String
  sourceExchangeRequestId String?                  @unique
  title                   String
  summary                 String?
  status                  SchoolAnnouncementStatus @default(DRAFT)
  policyVersion           String                   @default("school-radio-v1")
  createdByUserId         String
  reviewedByUserId        String?
  submittedAt             DateTime?
  reviewedAt              DateTime?
  reviewNotes             String?
  createdAt               DateTime                 @default(now())
  updatedAt               DateTime                 @updatedAt

  organisation          Organisation                  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  promoVersion          PromoVersion                  @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  sourceExchangeRequest SchoolEpisodeExchangeRequest? @relation(fields: [sourceExchangeRequestId], references: [id], onDelete: SetNull)
  createdBy             User                          @relation("SchoolAnnouncementCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  reviewedBy            User?                         @relation("SchoolAnnouncementReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  broadcastSlots        SchoolBroadcastSlot[]
  rundownItems          SchoolRundownItem[]
  noticeboardPosts      SchoolNoticeboardPost[]

  @@index([organisationId, status])
  @@index([promoVersionId])
  @@index([sourceExchangeRequestId])
  @@index([createdByUserId])
  @@index([reviewedByUserId])
}

model SchoolNoticeboardPost {
  id                 String                  @id @default(cuid())
  organisationId     String
  announcementId     String
  locationId         String?
  zoneId             String?
  status             SchoolNoticeboardStatus @default(SCHEDULED)
  theme              SchoolNoticeboardTheme  @default(INFORMATION)
  priority           Int                     @default(50)
  displaySeconds     Int                     @default(15)
  startsAt           DateTime
  endsAt             DateTime
  policyVersion      String                  @default("school-noticeboard-v1")
  createdByUserId    String
  cancelledByUserId  String?
  cancelledAt        DateTime?
  cancellationReason String?
  createdAt          DateTime                @default(now())
  updatedAt          DateTime                @updatedAt

  organisation Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  announcement SchoolAnnouncement @relation(fields: [announcementId], references: [id], onDelete: Restrict)
  location     Location?          @relation(fields: [locationId], references: [id], onDelete: Cascade)
  zone         Zone?              @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  createdBy    User               @relation("SchoolNoticeboardCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  cancelledBy  User?              @relation("SchoolNoticeboardCancelledBy", fields: [cancelledByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, startsAt, endsAt])
  @@index([announcementId])
  @@index([locationId, status, startsAt, endsAt])
  @@index([zoneId, status, startsAt, endsAt])
  @@index([createdByUserId])
  @@index([cancelledByUserId])
}

model SchoolBroadcastSlot {
  id                 String                    @id @default(cuid())
  organisationId     String
  announcementId     String?
  episodeId          String?
  locationId         String?
  zoneId             String?
  startsAt           DateTime
  endsAt             DateTime
  status             SchoolBroadcastSlotStatus @default(APPROVED)
  revision           Int                       @default(1)
  approvedByUserId   String
  approvedAt         DateTime                  @default(now())
  cancelledAt        DateTime?
  cancellationReason String?
  createdAt          DateTime                  @default(now())
  updatedAt          DateTime                  @updatedAt

  organisation      Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  announcement      SchoolAnnouncement? @relation(fields: [announcementId], references: [id], onDelete: Restrict)
  episode           SchoolEpisode?      @relation(fields: [episodeId], references: [id], onDelete: Restrict)
  location          Location?           @relation(fields: [locationId], references: [id], onDelete: Cascade)
  zone              Zone?               @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  approvedBy        User                @relation("SchoolBroadcastSlotApprovedBy", fields: [approvedByUserId], references: [id], onDelete: Restrict)
  playoutIntents    PlayoutIntent[]
  proofOfPlayEvents ProofOfPlayEvent[]

  @@index([organisationId, status, startsAt])
  @@index([locationId, status, startsAt])
  @@index([zoneId, status, startsAt])
  @@index([announcementId])
  @@index([episodeId])
  @@index([approvedByUserId])
}

model StaffSupervisor {
  id             String   @id @default(cuid())
  organisationId String
  userId         String
  displayTitle   String?
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organisation Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  user         User              @relation("SchoolSupervisorUser", fields: [userId], references: [id], onDelete: Cascade)
  groups       StudentGroup[]
  programmes   SchoolProgramme[]

  @@unique([organisationId, userId])
  @@index([organisationId, active])
}

model StudentGroup {
  id              String   @id @default(cuid())
  organisationId  String
  supervisorId    String
  name            String
  academicYear    String?
  notes           String?
  createdByUserId String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  organisation  Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  supervisor    StaffSupervisor      @relation(fields: [supervisorId], references: [id], onDelete: Restrict)
  createdBy     User                 @relation("StudentGroupCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  contributors  StudentContributor[]
  programmes    SchoolProgramme[]
  audioProjects AudioProject[]
  assignments   Assignment[]

  @@unique([organisationId, name, academicYear])
  @@index([organisationId, supervisorId])
}

model StudentContributor {
  id             String                   @id @default(cuid())
  organisationId String
  studentGroupId String
  displayName    String
  referenceCode  String?
  status         StudentContributorStatus @default(ACTIVE)
  createdAt      DateTime                 @default(now())
  updatedAt      DateTime                 @updatedAt

  organisation          Organisation                      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  studentGroup          StudentGroup                      @relation(fields: [studentGroupId], references: [id], onDelete: Cascade)
  episodes              SchoolEpisodeContributor[]
  consentRecords        ConsentRecord[]
  assignmentSubmissions AssignmentSubmissionContributor[]
  portfolioEntries      PortfolioEntry[]
  studentAccess         SchoolStudentAccess?

  @@unique([studentGroupId, displayName])
  @@index([organisationId, status])
}

model SchoolStudentAccess {
  id                  String                    @id @default(cuid())
  organisationId      String
  contributorId       String                    @unique
  userId              String?                   @unique
  email               String                    @unique
  status              SchoolStudentAccessStatus @default(INVITED)
  invitationTokenHash String?                   @unique
  invitationExpiresAt DateTime?
  invitedByUserId     String
  acceptedAt          DateTime?
  revokedAt           DateTime?
  createdAt           DateTime                  @default(now())
  updatedAt           DateTime                  @updatedAt

  organisation Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  contributor  StudentContributor @relation(fields: [contributorId], references: [id], onDelete: Cascade)
  user         User?              @relation("SchoolStudentAccessUser", fields: [userId], references: [id], onDelete: SetNull)
  invitedBy    User               @relation("SchoolStudentAccessInvitedBy", fields: [invitedByUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, status])
  @@index([invitedByUserId])
  @@index([invitationExpiresAt])
}

model SchoolProgramme {
  id              String                @id @default(cuid())
  organisationId  String
  studentGroupId  String?
  supervisorId    String
  title           String
  description     String?
  status          SchoolProgrammeStatus @default(ACTIVE)
  createdByUserId String
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt

  organisation  Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  studentGroup  StudentGroup?         @relation(fields: [studentGroupId], references: [id], onDelete: SetNull)
  supervisor    StaffSupervisor       @relation(fields: [supervisorId], references: [id], onDelete: Restrict)
  createdBy     User                  @relation("SchoolProgrammeCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  episodes      SchoolEpisode[]
  audioProjects AudioProject[]
  podcastSeries SchoolPodcastSeries[]
  newsStories   SchoolNewsStory[]
  liveSessions  LiveStudioSession[]
  assignments   Assignment[]

  @@unique([organisationId, title])
  @@index([organisationId, status])
  @@index([studentGroupId])
}

model SchoolEpisode {
  id               String                 @id @default(cuid())
  organisationId   String
  programmeId      String
  title            String
  summary          String?
  status           SchoolEpisodeStatus    @default(DRAFT)
  publicationScope SchoolPublicationScope @default(INTERNAL_ONLY)
  createdByUserId  String
  submittedAt      DateTime?
  approvedAt       DateTime?
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt

  organisation          Organisation                 @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  programme             SchoolProgramme              @relation(fields: [programmeId], references: [id], onDelete: Restrict)
  createdBy             User                         @relation("SchoolEpisodeCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  contributors          SchoolEpisodeContributor[]
  submissions           SchoolSubmission[]
  reviews               SchoolModerationReview[]
  consentRecords        ConsentRecord[]
  audioProjects         AudioProject[]
  rundown               SchoolRundown?
  broadcastSlots        SchoolBroadcastSlot[]
  podcastEpisode        SchoolPodcastEpisode?
  networkExchangeOffers SchoolEpisodeExchangeOffer[]
  transcripts           Transcript[]
  newsStories           SchoolNewsStory[]
  liveSessions          LiveStudioSession[]
  assignmentSubmissions AssignmentSubmission[]

  @@index([organisationId, status])
  @@index([programmeId, createdAt])
}

model SchoolRundown {
  id               String              @id @default(cuid())
  organisationId   String
  episodeId        String              @unique
  status           SchoolRundownStatus @default(DRAFT)
  revision         Int                 @default(1)
  approvedRevision Int?
  createdByUserId  String
  reviewedByUserId String?
  submittedAt      DateTime?
  reviewedAt       DateTime?
  reviewNotes      String?
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt

  organisation Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  episode      SchoolEpisode       @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  createdBy    User                @relation("SchoolRundownCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  reviewedBy   User?               @relation("SchoolRundownReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  items        SchoolRundownItem[]

  @@index([organisationId, status, updatedAt])
  @@index([reviewedByUserId])
}

model SchoolRundownItem {
  id                   String                @id @default(cuid())
  rundownId            String
  type                 SchoolRundownItemType
  position             Int
  label                String
  notes                String?
  sourceMediaAssetId   String?
  sourceTrackId        String?
  sourcePromoVersionId String?
  sourceAnnouncementId String?
  sourceTakeId         String?
  estimatedDurationMs  Int?
  introCueMs           Int                   @default(0)
  outroCueMs           Int                   @default(0)
  transitionPreset     String                @default("CLEAN")
  cueOffsetMs          Int?
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt

  rundown            SchoolRundown       @relation(fields: [rundownId], references: [id], onDelete: Cascade)
  sourceMediaAsset   MediaAsset?         @relation(fields: [sourceMediaAssetId], references: [id], onDelete: Restrict)
  sourceTrack        Track?              @relation(fields: [sourceTrackId], references: [id], onDelete: Restrict)
  sourcePromoVersion PromoVersion?       @relation(fields: [sourcePromoVersionId], references: [id], onDelete: Restrict)
  sourceAnnouncement SchoolAnnouncement? @relation(fields: [sourceAnnouncementId], references: [id], onDelete: Restrict)
  sourceTake         AudioTake?          @relation(fields: [sourceTakeId], references: [id], onDelete: Restrict)
  playoutIntents     PlayoutIntent[]

  @@unique([rundownId, position])
  @@index([sourceMediaAssetId])
  @@index([sourceTrackId])
  @@index([sourcePromoVersionId])
  @@index([sourceAnnouncementId])
  @@index([sourceTakeId])
}

model SchoolEpisodeContributor {
  episodeId     String
  contributorId String
  creditedAs    String?
  createdAt     DateTime @default(now())

  episode     SchoolEpisode      @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  contributor StudentContributor @relation(fields: [contributorId], references: [id], onDelete: Restrict)

  @@id([episodeId, contributorId])
  @@index([contributorId])
}

model SchoolSubmission {
  id                String                 @id @default(cuid())
  organisationId    String
  episodeId         String
  promoVersionId    String
  revision          Int
  notes             String?
  status            SchoolSubmissionStatus @default(SUBMITTED)
  submittedByUserId String
  submittedAt       DateTime               @default(now())
  createdAt         DateTime               @default(now())

  organisation Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  episode      SchoolEpisode            @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  promoVersion PromoVersion             @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  submittedBy  User                     @relation("SchoolSubmissionSubmittedBy", fields: [submittedByUserId], references: [id], onDelete: Restrict)
  reviews      SchoolModerationReview[]

  @@unique([episodeId, revision])
  @@index([organisationId, status])
  @@index([promoVersionId])
}

model SchoolModerationReview {
  id             String                   @id @default(cuid())
  organisationId String
  episodeId      String
  submissionId   String
  reviewerUserId String
  decision       SchoolModerationDecision
  notes          String?
  createdAt      DateTime                 @default(now())

  organisation Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  episode      SchoolEpisode    @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  submission   SchoolSubmission @relation(fields: [submissionId], references: [id], onDelete: Restrict)
  reviewer     User             @relation("SchoolModerationReviewer", fields: [reviewerUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, createdAt])
  @@index([episodeId, createdAt])
  @@index([submissionId])
}

model ConsentRecord {
  id               String              @id @default(cuid())
  organisationId   String
  contributorId    String
  episodeId        String?
  status           ConsentRecordStatus @default(PENDING)
  policyVersion    String              @default("school-radio-v1")
  notes            String?
  recordedByUserId String
  grantedAt        DateTime?
  expiresAt        DateTime?
  revokedAt        DateTime?
  createdAt        DateTime            @default(now())

  organisation Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  contributor  StudentContributor @relation(fields: [contributorId], references: [id], onDelete: Cascade)
  episode      SchoolEpisode?     @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  recordedBy   User               @relation("ConsentRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, contributorId, status])
  @@index([episodeId, status])
}

model AudioProject {
  id              String             @id @default(cuid())
  organisationId  String
  programmeId     String?
  episodeId       String?
  studentGroupId  String?
  title           String
  type            AudioProjectType   @default(QUICK_RECORD)
  status          AudioProjectStatus @default(DRAFT)
  currentVersion  Int                @default(1)
  editDecision    Json
  createdByUserId String
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt

  organisation          Organisation               @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  programme             SchoolProgramme?           @relation(fields: [programmeId], references: [id], onDelete: SetNull)
  episode               SchoolEpisode?             @relation(fields: [episodeId], references: [id], onDelete: SetNull)
  studentGroup          StudentGroup?              @relation(fields: [studentGroupId], references: [id], onDelete: SetNull)
  createdBy             User                       @relation("AudioProjectCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  versions              AudioProjectVersion[]
  takes                 AudioTake[]
  uploadSessions        SchoolAudioUploadSession[]
  tracks                AudioTrack[]
  markers               AudioMarker[]
  renders               AudioRender[]
  transcripts           Transcript[]
  assignmentSubmissions AssignmentSubmission[]

  @@index([organisationId, status])
  @@index([episodeId])
  @@index([createdByUserId, updatedAt])
}

model AudioProjectVersion {
  id              String   @id @default(cuid())
  projectId       String
  version         Int
  state           Json
  reason          String?
  createdByUserId String
  createdAt       DateTime @default(now())

  project   AudioProject  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  createdBy User          @relation("AudioProjectVersionCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  renders   AudioRender[]

  @@unique([projectId, version])
  @@index([createdByUserId, createdAt])
}

model Assignment {
  id              String           @id @default(cuid())
  organisationId  String
  studentGroupId  String
  programmeId     String?
  title           String
  brief           String?
  templateCode    String
  allowedTools    Json?
  dueAt           DateTime?
  status          AssignmentStatus @default(DRAFT)
  createdByUserId String
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  organisation Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  studentGroup StudentGroup           @relation(fields: [studentGroupId], references: [id], onDelete: Restrict)
  programme    SchoolProgramme?       @relation(fields: [programmeId], references: [id], onDelete: SetNull)
  createdBy    User                   @relation("AssignmentCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  rubric       Rubric?
  submissions  AssignmentSubmission[]

  @@index([organisationId, status, dueAt])
  @@index([studentGroupId, status])
  @@index([programmeId])
}

model Rubric {
  id           String   @id @default(cuid())
  assignmentId String   @unique
  title        String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  assignment Assignment        @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
  criteria   RubricCriterion[]
}

model RubricCriterion {
  id          String   @id @default(cuid())
  rubricId    String
  label       String
  description String?
  maxScore    Int
  position    Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  rubric           Rubric            @relation(fields: [rubricId], references: [id], onDelete: Cascade)
  assessmentScores AssessmentScore[]

  @@unique([rubricId, position])
  @@index([rubricId])
}

model AssignmentSubmission {
  id               String                     @id @default(cuid())
  organisationId   String
  assignmentId     String
  audioProjectId   String?
  episodeId        String?
  status           AssignmentSubmissionStatus @default(SUBMITTED)
  revision         Int                        @default(1)
  reflection       String?
  recordedByUserId String
  submittedAt      DateTime                   @default(now())
  createdAt        DateTime                   @default(now())
  updatedAt        DateTime                   @updatedAt

  organisation Organisation                      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  assignment   Assignment                        @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
  audioProject AudioProject?                     @relation(fields: [audioProjectId], references: [id], onDelete: SetNull)
  episode      SchoolEpisode?                    @relation(fields: [episodeId], references: [id], onDelete: SetNull)
  recordedBy   User                              @relation("AssignmentSubmissionRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)
  contributors AssignmentSubmissionContributor[]
  assessment   Assessment?
  portfolios   PortfolioEntry[]

  @@index([organisationId, status, submittedAt])
  @@index([assignmentId, revision])
  @@index([audioProjectId])
  @@index([episodeId])
}

model AssignmentSubmissionContributor {
  submissionId  String
  contributorId String
  projectRole   String?
  createdAt     DateTime @default(now())

  submission  AssignmentSubmission @relation(fields: [submissionId], references: [id], onDelete: Cascade)
  contributor StudentContributor   @relation(fields: [contributorId], references: [id], onDelete: Restrict)

  @@id([submissionId, contributorId])
  @@index([contributorId])
}

model Assessment {
  id               String           @id @default(cuid())
  organisationId   String
  submissionId     String           @unique
  status           AssessmentStatus @default(DRAFT)
  totalScore       Int
  maximumScore     Int
  narrativeNotes   String?
  revisionRequest  String?
  assessedByUserId String
  assessedAt       DateTime         @default(now())
  releasedAt       DateTime?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  organisation Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  submission   AssignmentSubmission   @relation(fields: [submissionId], references: [id], onDelete: Cascade)
  assessedBy   User                   @relation("AssessmentAssessedBy", fields: [assessedByUserId], references: [id], onDelete: Restrict)
  scores       AssessmentScore[]
  annotations  AssessmentAnnotation[]
  portfolios   PortfolioEntry[]

  @@index([organisationId, status, assessedAt])
  @@index([assessedByUserId])
}

model AssessmentScore {
  assessmentId String
  criterionId  String
  score        Int
  notes        String?

  assessment Assessment      @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  criterion  RubricCriterion @relation(fields: [criterionId], references: [id], onDelete: Restrict)

  @@id([assessmentId, criterionId])
  @@index([criterionId])
}

model AssessmentAnnotation {
  id           String   @id @default(cuid())
  assessmentId String
  positionMs   Int
  endMs        Int?
  note         String
  createdAt    DateTime @default(now())

  assessment Assessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)

  @@index([assessmentId, positionMs])
}

model PortfolioEntry {
  id              String               @id @default(cuid())
  organisationId  String
  contributorId   String
  submissionId    String
  assessmentId    String?
  title           String
  projectRole     String?
  reflection      String?
  skillsJson      Json?
  status          PortfolioEntryStatus @default(PRIVATE)
  createdByUserId String
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt

  organisation Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  contributor  StudentContributor   @relation(fields: [contributorId], references: [id], onDelete: Restrict)
  submission   AssignmentSubmission @relation(fields: [submissionId], references: [id], onDelete: Cascade)
  assessment   Assessment?          @relation(fields: [assessmentId], references: [id], onDelete: SetNull)
  createdBy    User                 @relation("PortfolioEntryCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)

  @@unique([submissionId, contributorId])
  @@index([organisationId, status, updatedAt])
  @@index([contributorId, status])
  @@index([assessmentId])
}

model AudioTake {
  id                  String              @id @default(cuid())
  organisationId      String
  projectId           String
  mediaAssetId        String
  promoVersionId      String?
  recordedByUserId    String
  deviceLabel         String?
  durationMs          Int?
  status              AudioTakeStatus     @default(PROCESSING)
  waveformStatus      AudioWaveformStatus @default(PENDING)
  waveformPeaks       Json?
  waveformGeneratedAt DateTime?
  sourceEditDecision  Json
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt

  organisation Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project      AudioProject        @relation(fields: [projectId], references: [id], onDelete: Cascade)
  mediaAsset   MediaAsset          @relation(fields: [mediaAssetId], references: [id], onDelete: Restrict)
  promoVersion PromoVersion?       @relation(fields: [promoVersionId], references: [id], onDelete: SetNull)
  recordedBy   User                @relation("AudioTakeRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)
  rundownItems SchoolRundownItem[]

  @@index([organisationId, createdAt])
  @@index([projectId, createdAt])
  @@index([mediaAssetId])
  @@index([promoVersionId])
  @@index([waveformStatus, createdAt])
}

model AudioTrack {
  id              String         @id @default(cuid())
  projectId       String
  kind            AudioTrackKind @default(VOICE)
  name            String
  order           Int            @default(0)
  gainDb          Float          @default(0)
  pan             Float          @default(0)
  muted           Boolean        @default(false)
  solo            Boolean        @default(false)
  armed           Boolean        @default(false)
  locked          Boolean        @default(false)
  effectChainJson Json?
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  project AudioProject @relation(fields: [projectId], references: [id], onDelete: Cascade)
  clips   AudioClip[]

  @@index([projectId, order])
}

model AudioClip {
  id              String        @id @default(cuid())
  trackId         String
  mediaAssetId    String?
  kind            AudioClipKind @default(SOURCE)
  sourceStartMs   Int           @default(0)
  sourceEndMs     Int
  timelineStartMs Int           @default(0)
  gainDb          Float         @default(0)
  fadeInMs        Int           @default(0)
  fadeOutMs       Int           @default(0)
  fadeInCurve     String        @default("linear")
  fadeOutCurve    String        @default("linear")
  locked          Boolean       @default(false)
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  track      AudioTrack  @relation(fields: [trackId], references: [id], onDelete: Cascade)
  mediaAsset MediaAsset? @relation(fields: [mediaAssetId], references: [id], onDelete: Restrict)

  @@index([trackId, timelineStartMs])
  @@index([mediaAssetId])
}

model AudioMarker {
  id              String          @id @default(cuid())
  projectId       String
  positionMs      Int
  type            AudioMarkerType
  label           String
  createdByUserId String
  createdAt       DateTime        @default(now())

  project   AudioProject @relation(fields: [projectId], references: [id], onDelete: Cascade)
  createdBy User         @relation("AudioMarkerCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)

  @@index([projectId, positionMs])
}

model AudioRender {
  id                   String            @id @default(cuid())
  organisationId       String
  projectId            String
  versionId            String
  outputMediaAssetId   String?
  outputPromoVersionId String?
  requestedByUserId    String
  preset               AudioRenderPreset
  status               AudioRenderStatus @default(QUEUED)
  loudnessLufs         Float?
  resultJson           Json?
  errorMessage         String?
  startedAt            DateTime?
  completedAt          DateTime?
  createdAt            DateTime          @default(now())
  updatedAt            DateTime          @updatedAt

  organisation       Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project            AudioProject        @relation(fields: [projectId], references: [id], onDelete: Cascade)
  version            AudioProjectVersion @relation(fields: [versionId], references: [id], onDelete: Restrict)
  outputMediaAsset   MediaAsset?         @relation("AudioRenderOutput", fields: [outputMediaAssetId], references: [id], onDelete: SetNull)
  outputPromoVersion PromoVersion?       @relation("AudioRenderPromoVersion", fields: [outputPromoVersionId], references: [id], onDelete: SetNull)
  requestedBy        User                @relation("AudioRenderRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, createdAt])
  @@index([projectId, createdAt])
  @@index([status, createdAt])
  @@index([versionId])
}

model SchoolPodcastSeries {
  id               String                 @id @default(cuid())
  organisationId   String
  programmeId      String?
  title            String
  description      String?
  publicationScope SchoolPublicationScope @default(INTERNAL_ONLY)
  rssEnabled       Boolean                @default(false)
  artworkUrl       String?
  createdByUserId  String
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt

  organisation Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  programme    SchoolProgramme?       @relation(fields: [programmeId], references: [id], onDelete: SetNull)
  createdBy    User                   @relation("SchoolPodcastSeriesCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  episodes     SchoolPodcastEpisode[]

  @@unique([organisationId, title])
  @@index([organisationId, updatedAt])
  @@index([programmeId])
}

model SchoolPodcastEpisode {
  id                       String                 @id @default(cuid())
  organisationId           String
  seriesId                 String
  episodeId                String                 @unique
  status                   SchoolPodcastStatus    @default(DRAFT)
  publicationScope         SchoolPublicationScope @default(INTERNAL_ONLY)
  accessibleDescription    String?
  chaptersJson             Json?
  createdByUserId          String
  reviewedByUserId         String?
  publishedAt              DateTime?
  unpublishedAt            DateTime?
  publicationRevision      Int                    @default(0)
  publicationPolicyVersion String?
  lastPolicyCheckAt        DateTime?
  unpublishReason          String?
  createdAt                DateTime               @default(now())
  updatedAt                DateTime               @updatedAt

  organisation               Organisation                      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  series                     SchoolPodcastSeries               @relation(fields: [seriesId], references: [id], onDelete: Cascade)
  episode                    SchoolEpisode                     @relation(fields: [episodeId], references: [id], onDelete: Restrict)
  createdBy                  User                              @relation("SchoolPodcastEpisodeCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  reviewedBy                 User?                             @relation("SchoolPodcastEpisodeReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  transcript                 Transcript?
  publicationDecisions       SchoolPublicationDecision[]
  publicationDailyAggregates SchoolPublicationDailyAggregate[]

  @@index([organisationId, status, updatedAt])
  @@index([seriesId, createdAt])
}

model SchoolPublicationDecision {
  id               String                        @id @default(cuid())
  organisationId   String
  podcastEpisodeId String
  actorUserId      String?
  decision         SchoolPublicationDecisionType
  policyVersion    String                        @default("school-publication-v1")
  policySnapshot   Json
  reason           String?
  createdAt        DateTime                      @default(now())

  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  podcastEpisode SchoolPodcastEpisode @relation(fields: [podcastEpisodeId], references: [id], onDelete: Cascade)
  actor          User?                @relation("SchoolPublicationDecisionActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, createdAt])
  @@index([podcastEpisodeId, createdAt])
  @@index([actorUserId])
}

model SchoolPublicationDailyAggregate {
  id                     String   @id @default(cuid())
  organisationId         String
  podcastEpisodeId       String
  bucketStart            DateTime
  metadataListingCount   Int      @default(0)
  audioRequestCount      Int      @default(0)
  audioBytesOffered      BigInt   @default(0)
  fullAudioRequestCount  Int      @default(0)
  rangeAudioRequestCount Int      @default(0)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  podcastEpisode SchoolPodcastEpisode @relation(fields: [podcastEpisodeId], references: [id], onDelete: Cascade)

  @@unique([organisationId, podcastEpisodeId, bucketStart])
  @@index([organisationId, bucketStart])
  @@index([podcastEpisodeId, bucketStart])
}

model Transcript {
  id               String           @id @default(cuid())
  organisationId   String
  podcastEpisodeId String?          @unique
  mediaAssetId     String?
  projectId        String?
  episodeId        String?
  languageCode     String           @default("und")
  segmentsJson     Json
  status           TranscriptStatus @default(DRAFT)
  source           String           @default("MANUAL")
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  podcastEpisode SchoolPodcastEpisode? @relation(fields: [podcastEpisodeId], references: [id], onDelete: Cascade)
  mediaAsset     MediaAsset?           @relation(fields: [mediaAssetId], references: [id], onDelete: SetNull)
  project        AudioProject?         @relation(fields: [projectId], references: [id], onDelete: SetNull)
  episode        SchoolEpisode?        @relation(fields: [episodeId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, updatedAt])
  @@index([mediaAssetId])
  @@index([projectId])
  @@index([episodeId])
}

model SchoolNewsStory {
  id                        String                @id @default(cuid())
  organisationId            String
  programmeId               String?
  episodeId                 String?
  title                     String
  type                      SchoolNewsStoryType
  status                    SchoolNewsStoryStatus @default(PITCH)
  deadline                  DateTime?
  pitch                     String?
  script                    String?
  factCheckNotes            String?
  sourcesJson               Json?
  interviewMediaAssetId     String?
  interviewConsentConfirmed Boolean               @default(false)
  editorialFeedbackJson     Json?
  createdByUserId           String
  assignedToUserId          String?
  reviewedByUserId          String?
  createdAt                 DateTime              @default(now())
  updatedAt                 DateTime              @updatedAt

  organisation        Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  programme           SchoolProgramme? @relation(fields: [programmeId], references: [id], onDelete: SetNull)
  episode             SchoolEpisode?   @relation(fields: [episodeId], references: [id], onDelete: SetNull)
  interviewMediaAsset MediaAsset?      @relation("SchoolNewsInterviewAsset", fields: [interviewMediaAssetId], references: [id], onDelete: SetNull)
  createdBy           User             @relation("SchoolNewsStoryCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  assignedTo          User?            @relation("SchoolNewsStoryAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)
  reviewedBy          User?            @relation("SchoolNewsStoryReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, deadline])
  @@index([programmeId])
  @@index([episodeId])
  @@index([interviewMediaAssetId])
}

model LiveStudioSession {
  id                     String                @id @default(cuid())
  organisationId         String
  programmeId            String
  episodeId              String?
  channelId              String
  fallbackPromoVersionId String
  supervisorUserId       String
  title                  String
  status                 LiveStudioStatus      @default(CREATED)
  connectionQuality      LiveConnectionQuality @default(UNKNOWN)
  soundcheckJson         Json?
  scheduledStart         DateTime
  scheduledEnd           DateTime
  connectionApprovedAt   DateTime?
  goLiveTokenHash        String?
  goLiveTokenExpiresAt   DateTime?
  liveStartedAt          DateTime?
  fallbackActivatedAt    DateTime?
  endedAt                DateTime?
  recordEnabled          Boolean               @default(false)
  retentionApproved      Boolean               @default(false)
  recordingMediaAssetId  String?
  endReason              String?
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @updatedAt

  organisation         Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  programme            SchoolProgramme @relation(fields: [programmeId], references: [id], onDelete: Restrict)
  episode              SchoolEpisode?  @relation(fields: [episodeId], references: [id], onDelete: SetNull)
  channel              Channel         @relation(fields: [channelId], references: [id], onDelete: Restrict)
  fallbackPromoVersion PromoVersion    @relation(fields: [fallbackPromoVersionId], references: [id], onDelete: Restrict)
  supervisor           User            @relation("LiveStudioSupervisor", fields: [supervisorUserId], references: [id], onDelete: Restrict)
  recordingMediaAsset  MediaAsset?     @relation("LiveStudioRecordingAsset", fields: [recordingMediaAssetId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, scheduledStart])
  @@index([programmeId])
  @@index([episodeId])
  @@index([channelId])
  @@index([fallbackPromoVersionId])
  @@index([supervisorUserId])
}

model SchoolAudioUploadSession {
  id                String            @id @default(cuid())
  organisationId    String
  projectId         String
  status            AudioUploadStatus @default(INITIATED)
  originalName      String
  mimeType          String
  expectedSizeBytes BigInt
  partSizeBytes     Int
  partCount         Int
  quarantineKey     String            @unique
  multipartUploadId String            @unique
  createdByUserId   String
  expiresAt         DateTime
  completedAt       DateTime?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt

  organisation Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project      AudioProject            @relation(fields: [projectId], references: [id], onDelete: Cascade)
  createdBy    User                    @relation("SchoolAudioUploadCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  parts        SchoolAudioUploadPart[]

  @@index([organisationId, status])
  @@index([projectId, createdAt])
  @@index([expiresAt, status])
}

model SchoolAudioUploadPart {
  id         String   @id @default(cuid())
  sessionId  String
  partNumber Int
  eTag       String
  sizeBytes  Int
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  session SchoolAudioUploadSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@unique([sessionId, partNumber])
  @@index([sessionId])
}

model ProductionOrder {
  id                    String                  @id @default(cuid())
  organisationId        String
  createdByUserId       String
  assignedToUserId      String?
  title                 String
  promotionDetails      String
  mandatoryLegalWording String?
  languageCodes         String[]
  voicePreference       String?
  toneStyle             String?
  targetDurationSeconds Int?
  musicBedPreference    String?
  campaignStartsOn      DateTime?               @db.Date
  campaignEndsOn        DateTime?               @db.Date
  pronunciationNotes    String?
  contactName           String
  contactEmail          String
  fundingType           ProductionFundingType
  fundingStatus         ProductionFundingStatus @default(PENDING)
  promoAssetId          String?                 @unique
  priority              ProductionOrderPriority @default(STANDARD)
  deadlineAt            DateTime?
  status                ProductionOrderStatus   @default(DRAFT)
  submittedAt           DateTime?
  customerApprovedAt    DateTime?
  deliveredAt           DateTime?
  cancelledAt           DateTime?
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt

  organisation  Organisation                  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy     User                          @relation("ProductionOrderCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  assignedTo    User?                         @relation("ProductionOrderAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)
  events        ProductionOrderEvent[]
  files         ProductionOrderFile[]
  scripts       ProductionScriptVersion[]
  revisions     ProductionRevisionRequest[]
  creditEntries ProductionCreditLedgerEntry[]
  promoAsset    PromoAsset?                   @relation("ProductionOrderPromoAsset", fields: [promoAssetId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, createdAt])
  @@index([createdByUserId])
  @@index([assignedToUserId, status])
  @@index([organisationId, fundingStatus])
  @@index([deadlineAt])
}

model ProductionCreditLedgerEntry {
  id                String                    @id @default(cuid())
  organisationId    String
  orderId           String?
  actorUserId       String?
  sequence          Int
  entryType         ProductionCreditEntryType
  quantity          Int
  availableDelta    Int
  reservedDelta     Int
  availableAfter    Int
  reservedAfter     Int
  idempotencyKey    String                    @unique
  externalReference String?
  note              String?
  expiresAt         DateTime?
  createdAt         DateTime                  @default(now())

  organisation Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  order        ProductionOrder? @relation(fields: [orderId], references: [id], onDelete: Restrict)
  actor        User?            @relation("ProductionCreditEntryActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@unique([organisationId, sequence])
  @@index([organisationId, createdAt])
  @@index([orderId, createdAt])
  @@index([actorUserId, createdAt])
  @@index([entryType, createdAt])
}

model ProductionOrderFile {
  id               String                  @id @default(cuid())
  organisationId   String
  orderId          String
  uploadedByUserId String
  kind             ProductionOrderFileKind
  originalName     String
  storageKey       String                  @unique
  mimeType         String
  sizeBytes        BigInt
  checksumSha256   String
  createdAt        DateTime                @default(now())

  organisation Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  order        ProductionOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  uploadedBy   User            @relation("ProductionOrderFileUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, createdAt])
  @@index([orderId, kind, createdAt])
  @@index([uploadedByUserId, createdAt])
}

model ProductionScriptVersion {
  id              String   @id @default(cuid())
  organisationId  String
  orderId         String
  createdByUserId String
  version         Int
  languageCode    String
  content         String
  productionNotes String?
  createdAt       DateTime @default(now())

  organisation Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  order        ProductionOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  createdBy    User            @relation("ProductionScriptVersionCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)

  @@unique([orderId, version])
  @@index([organisationId, createdAt])
  @@index([createdByUserId, createdAt])
}

model ProductionRevisionRequest {
  id                String                   @id @default(cuid())
  organisationId    String
  orderId           String
  requestedByUserId String
  resolvedByUserId  String?
  message           String
  status            ProductionRevisionStatus @default(OPEN)
  createdAt         DateTime                 @default(now())
  resolvedAt        DateTime?

  organisation Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  order        ProductionOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  requestedBy  User            @relation("ProductionRevisionRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)
  resolvedBy   User?           @relation("ProductionRevisionResolvedBy", fields: [resolvedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, createdAt])
  @@index([orderId, status, createdAt])
  @@index([requestedByUserId, createdAt])
}

model ProductionOrderEvent {
  id             String                   @id @default(cuid())
  organisationId String
  orderId        String
  actorUserId    String?
  eventType      ProductionOrderEventType
  fromStatus     ProductionOrderStatus?
  toStatus       ProductionOrderStatus?
  note           String?
  createdAt      DateTime                 @default(now())

  organisation Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  order        ProductionOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  actor        User?           @relation("ProductionOrderEventActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, createdAt])
  @@index([orderId, createdAt])
  @@index([actorUserId, createdAt])
}

model MusicSchedule {
  id             String              @id @default(cuid())
  organisationId String
  locationId     String?
  zoneId         String?
  name           String
  version        Int                 @default(1)
  status         MusicScheduleStatus @default(DRAFT)
  timezone       String
  effectiveFrom  DateTime?           @db.Date
  effectiveTo    DateTime?           @db.Date
  publishedAt    DateTime?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt

  organisation Organisation   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  location     Location?      @relation(fields: [locationId], references: [id], onDelete: Cascade)
  zone         Zone?          @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  slots        ScheduleSlot[]

  @@index([organisationId, status])
  @@index([locationId, status, version])
  @@index([zoneId, status, version])
}

model ScheduleSlot {
  id          String   @id @default(cuid())
  scheduleId  String
  musicModeId String
  weekday     Int
  startMinute Int
  endMinute   Int
  priority    Int      @default(0)
  createdAt   DateTime @default(now())

  schedule  MusicSchedule @relation(fields: [scheduleId], references: [id], onDelete: Cascade)
  musicMode MusicMode     @relation(fields: [musicModeId], references: [id], onDelete: Restrict)

  @@index([scheduleId, weekday, startMinute])
  @@index([musicModeId])
}

model MediaGenre {
  id        String   @id @default(cuid())
  name      String   @unique
  slug      String   @unique
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  mediaAssets MediaAssetGenre[]
}

model MediaAssetGenre {
  mediaAssetId String
  mediaGenreId String
  isPrimary    Boolean  @default(false)
  createdAt    DateTime @default(now())

  mediaAsset MediaAsset @relation(fields: [mediaAssetId], references: [id], onDelete: Cascade)
  mediaGenre MediaGenre @relation(fields: [mediaGenreId], references: [id], onDelete: Cascade)

  @@id([mediaAssetId, mediaGenreId])
  @@index([mediaGenreId])
  @@index([mediaAssetId])
}

model AuditLog {
  id                    String   @id @default(cuid())
  organisationId        String?
  schoolNetworkId       String?
  actorUserId           String?
  actorServiceAccountId String?
  action                String
  entityType            String
  entityId              String?
  details               Json?
  createdAt             DateTime @default(now())

  organisation        Organisation?   @relation(fields: [organisationId], references: [id], onDelete: SetNull)
  schoolNetwork       SchoolNetwork?  @relation(fields: [schoolNetworkId], references: [id], onDelete: SetNull)
  actor               User?           @relation("AuditActor", fields: [actorUserId], references: [id], onDelete: SetNull)
  actorServiceAccount ServiceAccount? @relation(fields: [actorServiceAccountId], references: [id], onDelete: SetNull)

  @@index([organisationId])
  @@index([schoolNetworkId])
  @@index([actorUserId])
  @@index([actorServiceAccountId])
}

model Job {
  id               String    @id @default(cuid())
  organisationId   String?
  type             JobType
  status           JobStatus @default(QUEUED)
  priority         Int       @default(0)
  payload          Json
  result           Json?
  idempotencyKey   String?
  correlationId    String
  requestId        String?
  attempts         Int       @default(0)
  maxAttempts      Int       @default(5)
  availableAt      DateTime  @default(now())
  leaseToken       String?
  leaseOwner       String?
  leaseUntil       DateTime?
  startedAt        DateTime?
  completedAt      DateTime?
  deadLetteredAt   DateTime?
  lastErrorCode    String?
  lastErrorMessage String?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  organisation      Organisation?      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  notificationEvent NotificationEvent? @relation("NotificationDeliveryJob")

  @@unique([organisationId, type, idempotencyKey])
  @@index([status, availableAt, priority])
  @@index([leaseUntil])
  @@index([organisationId, createdAt])
  @@index([correlationId])
}

model OperationalServiceHeartbeat {
  id          String                 @id @default(cuid())
  service     OperationalServiceKind
  environment String
  instanceId  String
  version     String
  commitSha   String?
  startedAt   DateTime
  lastSeenAt  DateTime
  details     Json?
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt

  @@unique([service, environment, instanceId])
  @@index([service, environment, lastSeenAt])
  @@index([version, lastSeenAt])
}

model RecoveryControl {
  id                       String            @id @default(cuid())
  assetKind                RecoveryAssetKind
  environment              String
  strategyConfirmed        Boolean           @default(false)
  automatedBackupConfirmed Boolean           @default(false)
  versioningConfirmed      Boolean           @default(false)
  targetRpoMinutes         Int?
  targetRtoMinutes         Int?
  retentionDays            Int?
  notes                    String
  reviewedAt               DateTime
  nextReviewAt             DateTime
  updatedByUserId          String
  createdAt                DateTime          @default(now())
  updatedAt                DateTime          @updatedAt

  updatedBy User               @relation("RecoveryControlUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: Restrict)
  evidence  RecoveryEvidence[]

  @@unique([assetKind, environment])
  @@unique([id, assetKind, environment])
  @@index([environment, strategyConfirmed])
  @@index([nextReviewAt])
}

model RecoveryEvidence {
  id                      String                 @id @default(cuid())
  recoveryControlId       String
  assetKind               RecoveryAssetKind
  environment             String
  evidenceKind            RecoveryEvidenceKind
  result                  RecoveryEvidenceResult
  evidenceReference       String
  performedAt             DateTime
  backupCapturedAt        DateTime?
  restoreCompletedMinutes Int?
  notes                   String
  recordedByUserId        String
  createdAt               DateTime               @default(now())

  recoveryControl RecoveryControl @relation(fields: [recoveryControlId, assetKind, environment], references: [id, assetKind, environment], onDelete: Restrict)
  recordedBy      User            @relation("RecoveryEvidenceRecordedBy", fields: [recordedByUserId], references: [id], onDelete: Restrict)

  @@unique([environment, evidenceReference])
  @@index([assetKind, environment, evidenceKind, performedAt])
  @@index([result, performedAt])
  @@index([recordedByUserId])
}

model NotificationEvent {
  id               String               @id @default(cuid())
  organisationId   String
  type             NotificationType
  severity         NotificationSeverity @default(INFO)
  title            String
  message          String
  entityType       String?
  entityId         String?
  metadata         Json?
  dedupeKey        String?
  correlationId    String
  deliveryJobId    String?              @unique
  occurredAt       DateTime             @default(now())
  deliveryQueuedAt DateTime?
  dispatchedAt     DateTime?
  createdAt        DateTime             @default(now())

  organisation Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  deliveryJob  Job?                   @relation("NotificationDeliveryJob", fields: [deliveryJobId], references: [id], onDelete: SetNull)
  deliveries   NotificationDelivery[]

  @@unique([organisationId, dedupeKey])
  @@index([organisationId, occurredAt])
  @@index([type, occurredAt])
  @@index([correlationId])
}

model NotificationPreference {
  id             String              @id @default(cuid())
  organisationId String
  userId         String
  type           NotificationType
  channel        NotificationChannel @default(IN_APP)
  enabled        Boolean             @default(true)
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([organisationId, userId, type, channel])
  @@index([userId, organisationId])
}

model NotificationDelivery {
  id                  String                     @id @default(cuid())
  organisationId      String
  notificationEventId String
  userId              String
  channel             NotificationChannel        @default(IN_APP)
  status              NotificationDeliveryStatus @default(PENDING)
  deliveredAt         DateTime?
  readAt              DateTime?
  dismissedAt         DateTime?
  failureCode         String?
  createdAt           DateTime                   @default(now())
  updatedAt           DateTime                   @updatedAt

  organisation      Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  notificationEvent NotificationEvent @relation(fields: [notificationEventId], references: [id], onDelete: Cascade)
  user              User              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([notificationEventId, userId, channel])
  @@index([userId, organisationId, readAt, createdAt])
  @@index([organisationId, status, createdAt])
}

model Session {
  id                   String    @id @default(cuid())
  userId               String
  activeOrganisationId String?
  tokenHash            String    @unique
  expiresAt            DateTime
  lastSeenAt           DateTime  @default(now())
  revokedAt            DateTime?
  authMethod           String    @default("PASSWORD")
  createdAt            DateTime  @default(now())

  user               User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  activeOrganisation Organisation? @relation(fields: [activeOrganisationId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([activeOrganisationId])
  @@index([expiresAt])
  @@index([revokedAt])
}

model EnterpriseSecurityPolicy {
  id                   String   @id @default(cuid())
  organisationId       String   @unique
  ssoRequired          Boolean  @default(false)
  passwordFallback     Boolean  @default(true)
  sessionMaxAgeMinutes Int      @default(43200)
  idleTimeoutMinutes   Int      @default(1440)
  allowedEmailDomains  String[] @default([])
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
}

model EnterpriseIdentityProvider {
  id             String                           @id @default(cuid())
  organisationId String
  name           String
  protocol       EnterpriseIdentityProtocol
  status         EnterpriseIdentityProviderStatus @default(DRAFT)
  issuer         String
  clientId       String?
  metadataUrl    String?
  emailDomain    String?
  createdAt      DateTime                         @default(now())
  updatedAt      DateTime                         @updatedAt

  organisation  Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  identityLinks EnterpriseIdentityLink[]

  @@unique([organisationId, issuer])
  @@index([organisationId, status])
}

model EnterpriseIdentityLink {
  id                  String    @id @default(cuid())
  providerId          String
  userId              String
  subject             String
  emailAtProvider     String?
  lastAuthenticatedAt DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  provider EnterpriseIdentityProvider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  user     User                       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([providerId, subject])
  @@unique([providerId, userId])
  @@index([userId])
}

model ServiceAccount {
  id              String               @id @default(cuid())
  organisationId  String
  createdByUserId String
  name            String
  description     String?
  status          ServiceAccountStatus @default(ACTIVE)
  scopes          String[]             @default([])
  expiresAt       DateTime?
  lastUsedAt      DateTime?
  revokedAt       DateTime?
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy    User         @relation("ServiceAccountCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  apiKeys      ApiKey[]
  auditLogs    AuditLog[]

  @@unique([organisationId, name])
  @@index([organisationId, status])
  @@index([createdByUserId])
}

model ApiKey {
  id               String       @id @default(cuid())
  serviceAccountId String
  name             String
  prefix           String
  tokenHash        String       @unique
  status           ApiKeyStatus @default(ACTIVE)
  expiresAt        DateTime?
  lastUsedAt       DateTime?
  revokedAt        DateTime?
  createdAt        DateTime     @default(now())

  serviceAccount ServiceAccount @relation(fields: [serviceAccountId], references: [id], onDelete: Cascade)

  @@index([serviceAccountId, status])
  @@index([prefix])
}

model IntegrationConnection {
  id                   String                      @id @default(cuid())
  organisationId       String
  createdByUserId      String
  name                 String
  kind                 IntegrationKind             @default(OUTGOING_WEBHOOK)
  providerKey          String                      @default("GENERIC_WEBHOOK_V1")
  status               IntegrationConnectionStatus @default(CONNECTED)
  endpointUrl          String?
  encryptedSecret      String?
  subscribedEventTypes String[]                    @default([])
  configuration        Json?
  lastSuccessfulSyncAt DateTime?
  lastErrorAt          DateTime?
  lastErrorMessage     String?
  disconnectedAt       DateTime?
  revokedAt            DateTime?
  createdAt            DateTime                    @default(now())
  updatedAt            DateTime                    @updatedAt

  organisation    Organisation               @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy       User                       @relation("IntegrationConnectionCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  syncRuns        IntegrationSyncRun[]
  metricSummaries IntegrationMetricSummary[]
  events          OutgoingWebhookEvent[]

  @@unique([organisationId, name])
  @@index([organisationId, status])
  @@index([kind, status])
}

model IntegrationSyncRun {
  id              String                   @id @default(cuid())
  organisationId  String
  connectionId    String
  status          IntegrationSyncRunStatus @default(PENDING)
  sourceTimestamp DateTime?
  summary         Json?
  errorMessage    String?
  startedAt       DateTime?
  completedAt     DateTime?
  createdAt       DateTime                 @default(now())

  organisation Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  connection   IntegrationConnection @relation(fields: [connectionId], references: [id], onDelete: Cascade)

  @@index([organisationId, createdAt])
  @@index([connectionId, status, createdAt])
}

model IntegrationMetricSummary {
  id              String                @id @default(cuid())
  organisationId  String
  connectionId    String
  locationId      String
  externalId      String
  metricType      IntegrationMetricType
  value           Decimal               @db.Decimal(20, 4)
  unit            String
  windowStartedAt DateTime
  windowEndedAt   DateTime
  sourceTimestamp DateTime
  dimensions      Json?
  createdAt       DateTime              @default(now())

  organisation Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  connection   IntegrationConnection @relation(fields: [connectionId], references: [id], onDelete: Cascade)
  location     Location              @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([connectionId, externalId])
  @@index([organisationId, sourceTimestamp])
  @@index([locationId, metricType, windowStartedAt])
  @@index([connectionId, createdAt])
}

model OutgoingWebhookEvent {
  id              String                @id @default(cuid())
  organisationId  String
  connectionId    String
  eventType       String
  idempotencyKey  String
  payload         Json
  status          WebhookDeliveryStatus @default(PENDING)
  attemptCount    Int                   @default(0)
  recoveryCount   Int                   @default(0)
  nextAttemptAt   DateTime              @default(now())
  deliveredAt     DateTime?
  lastRecoveredAt DateTime?
  lastError       String?
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt

  organisation Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  connection   IntegrationConnection    @relation(fields: [connectionId], references: [id], onDelete: Cascade)
  attempts     WebhookDeliveryAttempt[]

  @@unique([connectionId, idempotencyKey])
  @@index([organisationId, status, nextAttemptAt])
  @@index([connectionId, createdAt])
}

model WebhookDeliveryAttempt {
  id             String   @id @default(cuid())
  eventId        String
  attemptNumber  Int
  requestSha256  String
  responseStatus Int?
  errorMessage   String?
  attemptedAt    DateTime @default(now())

  event OutgoingWebhookEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@unique([eventId, attemptNumber])
  @@index([eventId, attemptedAt])
}

model RateLimitBucket {
  key             String   @id
  count           Int      @default(0)
  windowStartedAt DateTime
  expiresAt       DateTime
  updatedAt       DateTime @updatedAt

  @@index([expiresAt])
}

model AIJob {
  id                      String               @id @default(cuid())
  organisationId          String
  requestedByUserId       String
  reviewedByUserId        String?
  assistantType           AIAssistantType
  status                  AIJobStatus          @default(NEEDS_REVIEW)
  dataClassification      AIDataClassification @default(INTERNAL)
  providerKey             String               @default("RUVANAS_TEMPLATE_V1")
  providerDataUseApproved Boolean              @default(false)
  privateDataSent         Boolean              @default(false)
  input                   Json
  draftText               String
  approvedText            String?
  reviewNote              String?
  reviewedAt              DateTime?
  createdAt               DateTime             @default(now())
  updatedAt               DateTime             @updatedAt

  organisation Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  requestedBy  User                     @relation("AIJobRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)
  reviewedBy   User?                    @relation("AIJobReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  metadata     AIArtifactMetadata?
  feedback     RecommendationFeedback[]

  @@index([organisationId, status, createdAt])
  @@index([requestedByUserId])
  @@index([reviewedByUserId])
}

model AIArtifactMetadata {
  id          String   @id @default(cuid())
  aiJobId     String   @unique
  providerKey String
  modelKey    String
  generatedAt DateTime @default(now())
  provenance  Json

  aiJob AIJob @relation(fields: [aiJobId], references: [id], onDelete: Cascade)
}

model RecommendationFeedback {
  id        String   @id @default(cuid())
  aiJobId   String
  userId    String
  decision  String
  comment   String?
  createdAt DateTime @default(now())

  aiJob AIJob @relation(fields: [aiJobId], references: [id], onDelete: Cascade)
  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([aiJobId, userId])
  @@index([userId])
}

model Player {
  id                 String       @id @default(cuid())
  organisationId     String
  zoneId             String
  name               String
  status             PlayerStatus @default(PENDING_ENROLMENT)
  enrolmentTokenHash String?      @unique
  enrolmentExpiresAt DateTime?
  sessionTokenHash   String?      @unique
  enrolledAt         DateTime?
  lastHeartbeatAt    DateTime?
  lastIpAddress      String?
  lastUserAgent      String?
  sessionRevokedAt   DateTime?
  retiredAt          DateTime?
  retiredReason      String?
  replacesPlayerId   String?      @unique
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt

  organisation      Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  zone              Zone                    @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  proofOfPlayEvents ProofOfPlayEvent[]
  playoutIntents    PlayoutIntent[]
  heartbeatSamples  PlayerHeartbeatSample[]
  healthIncidents   PlayerHealthIncident[]
  commands          PlayerCommand[]
  listenerLeases    PlayerListenerLease[]
  replacesPlayer    Player?                 @relation("PlayerReplacement", fields: [replacesPlayerId], references: [id], onDelete: SetNull)
  replacementPlayer Player?                 @relation("PlayerReplacement")

  @@index([organisationId])
  @@index([zoneId])
  @@index([organisationId, status])
  @@index([lastHeartbeatAt])
  @@index([retiredAt])
}

model PlayerListenerLease {
  id             String    @id @default(cuid())
  organisationId String
  playerId       String
  instanceHash   String    @db.Char(64)
  lastSeenAt     DateTime  @default(now())
  expiresAt      DateTime
  revokedAt      DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player       Player       @relation(fields: [playerId], references: [id], onDelete: Cascade)

  @@unique([playerId, instanceHash])
  @@index([organisationId, revokedAt, expiresAt])
  @@index([playerId, expiresAt])
}

model PlayerCommand {
  id             String              @id @default(cuid())
  organisationId String
  playerId       String
  kind           PlayerCommandKind
  status         PlayerCommandStatus @default(PENDING)
  requestedById  String
  requestedAt    DateTime            @default(now())
  expiresAt      DateTime
  deliveredAt    DateTime?
  acknowledgedAt DateTime?
  resultCode     String?
  resultMessage  String?
  resultDetails  Json?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player       Player       @relation(fields: [playerId], references: [id], onDelete: Cascade)
  requestedBy  User         @relation("PlayerCommandRequestedBy", fields: [requestedById], references: [id], onDelete: Restrict)

  @@index([playerId, status, expiresAt])
  @@index([organisationId, requestedAt])
  @@index([requestedById, requestedAt])
  @@index([expiresAt])
}

model PlayerHeartbeatSample {
  id              String                    @id @default(cuid())
  organisationId  String
  playerId        String
  locationId      String
  zoneId          String
  kind            PlayerHeartbeatSampleKind @default(PERIODIC)
  bucketStart     DateTime
  observedAt      DateTime
  appVersion      String?
  manifestVersion String?
  sourceStatus    String?
  createdAt       DateTime                  @default(now())

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player       Player       @relation(fields: [playerId], references: [id], onDelete: Cascade)

  @@unique([playerId, bucketStart])
  @@index([organisationId, observedAt])
  @@index([locationId, observedAt])
  @@index([zoneId, observedAt])
}

model PlayerHealthIncident {
  id                  String                       @id @default(cuid())
  organisationId      String
  playerId            String
  locationId          String
  zoneId              String
  kind                PlayerHealthIncidentKind     @default(HEARTBEAT_MISSED)
  severity            PlayerHealthIncidentSeverity @default(LOW)
  status              PlayerHealthIncidentStatus   @default(OPEN)
  firstObservedAt     DateTime
  lastObservedAt      DateTime
  summary             String
  details             Json?
  acknowledgedAt      DateTime?
  acknowledgedById    String?
  acknowledgementNote String?
  resolutionNote      String?
  resolvedAt          DateTime?
  resolvedById        String?
  createdAt           DateTime                     @default(now())
  updatedAt           DateTime                     @updatedAt

  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player         Player       @relation(fields: [playerId], references: [id], onDelete: Cascade)
  acknowledgedBy User?        @relation("PlayerHealthIncidentAcknowledgedBy", fields: [acknowledgedById], references: [id], onDelete: SetNull)
  resolvedBy     User?        @relation("PlayerHealthIncidentResolvedBy", fields: [resolvedById], references: [id], onDelete: SetNull)

  @@index([organisationId, status, severity])
  @@index([playerId, status])
  @@index([locationId, firstObservedAt])
  @@index([zoneId, firstObservedAt])
  @@index([firstObservedAt])
}

model PlayoutIntent {
  id                    String   @id @default(cuid())
  scheduleItemId        String   @unique
  organisationId        String
  playerId              String
  zoneId                String
  channelId             String?
  campaignId            String?
  schoolBroadcastSlotId String?
  schoolRundownItemId   String?
  promoVersionId        String?
  mediaAssetId          String
  locationId            String
  locationName          String
  locationTimezone      String
  locationGroups        Json
  publicationRevision   Int
  sourceRevision        String
  plannedStart          DateTime
  expiresAt             DateTime
  createdAt             DateTime @default(now())

  organisation        Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player              Player               @relation(fields: [playerId], references: [id], onDelete: Cascade)
  zone                Zone                 @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  channel             Channel?             @relation(fields: [channelId], references: [id], onDelete: SetNull)
  campaign            Campaign?            @relation(fields: [campaignId], references: [id], onDelete: Restrict)
  schoolBroadcastSlot SchoolBroadcastSlot? @relation(fields: [schoolBroadcastSlotId], references: [id], onDelete: Restrict)
  schoolRundownItem   SchoolRundownItem?   @relation(fields: [schoolRundownItemId], references: [id], onDelete: Restrict)
  promoVersion        PromoVersion?        @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  mediaAsset          MediaAsset           @relation(fields: [mediaAssetId], references: [id], onDelete: Restrict)
  playbackEvents      ProofOfPlayEvent[]

  @@unique([playerId, campaignId, publicationRevision, plannedStart])
  @@unique([playerId, schoolBroadcastSlotId, plannedStart])
  @@index([organisationId, plannedStart])
  @@index([playerId, plannedStart])
  @@index([zoneId, plannedStart])
  @@index([channelId, plannedStart])
  @@index([campaignId, plannedStart])
  @@index([schoolBroadcastSlotId, plannedStart])
  @@index([schoolRundownItemId, plannedStart])
  @@index([promoVersionId, plannedStart])
  @@index([locationId, plannedStart])
  @@index([expiresAt])
}

model ProofOfPlayEvent {
  id                    String               @id @default(cuid())
  clientEventId         String
  organisationId        String
  playerId              String
  zoneId                String
  scheduleItemId        String
  itemType              PlaybackItemType     @default(MUSIC)
  trackId               String?
  campaignId            String?
  promoVersionId        String?
  playoutIntentId       String?
  schoolBroadcastSlotId String?
  channelId             String?
  mediaAssetId          String
  manifestVersion       String
  eventType             ProofOfPlayEventType
  occurredAt            DateTime
  receivedAt            DateTime             @default(now())
  positionSeconds       Int?
  failureReason         String?
  playerName            String
  locationName          String
  zoneName              String
  trackTitle            String
  trackArtist           String

  organisation        Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  player              Player               @relation(fields: [playerId], references: [id], onDelete: Cascade)
  zone                Zone                 @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  track               Track?               @relation(fields: [trackId], references: [id], onDelete: Restrict)
  campaign            Campaign?            @relation(fields: [campaignId], references: [id], onDelete: Restrict)
  promoVersion        PromoVersion?        @relation(fields: [promoVersionId], references: [id], onDelete: Restrict)
  playoutIntent       PlayoutIntent?       @relation(fields: [playoutIntentId], references: [id], onDelete: Restrict)
  schoolBroadcastSlot SchoolBroadcastSlot? @relation(fields: [schoolBroadcastSlotId], references: [id], onDelete: Restrict)
  channel             Channel?             @relation(fields: [channelId], references: [id], onDelete: SetNull)
  mediaAsset          MediaAsset           @relation(fields: [mediaAssetId], references: [id], onDelete: Restrict)

  @@unique([clientEventId])
  @@index([organisationId, occurredAt])
  @@index([playerId, occurredAt])
  @@index([scheduleItemId])
  @@index([trackId, occurredAt])
  @@index([campaignId, occurredAt])
  @@index([promoVersionId, occurredAt])
  @@index([playoutIntentId, occurredAt])
  @@index([schoolBroadcastSlotId, occurredAt])
  @@index([channelId, occurredAt])
  @@index([manifestVersion])
  @@index([eventType, occurredAt])
}

model ReportExportJob {
  id                String             @id @default(cuid())
  organisationId    String
  requestedByUserId String
  status            ReportExportStatus @default(QUEUED)
  reportType        String             @default("CAMPAIGN_PROOF_CSV")
  filters           Json
  csvContent        String?            @db.Text
  contentSha256     String?
  rowCount          Int                @default(0)
  attempts          Int                @default(0)
  availableAt       DateTime           @default(now())
  leaseUntil        DateTime?
  startedAt         DateTime?
  completedAt       DateTime?
  expiresAt         DateTime
  errorMessage      String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  organisation Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  requestedBy  User             @relation("ReportExportRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)
  auditSeal    AuditExportSeal?

  @@index([organisationId, createdAt])
  @@index([requestedByUserId, createdAt])
  @@index([status, availableAt, leaseUntil])
  @@index([expiresAt])
}

model CompliancePolicy {
  id              String   @id @default(cuid())
  key             String
  version         String
  title           String
  contentSha256   String
  effectiveAt     DateTime
  active          Boolean  @default(true)
  createdByUserId String?
  createdAt       DateTime @default(now())

  createdBy   User?              @relation("CompliancePolicyCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  acceptances PolicyAcceptance[]

  @@unique([key, version])
  @@index([active, effectiveAt])
}

model PolicyAcceptance {
  id               String   @id @default(cuid())
  organisationId   String
  policyId         String
  acceptedByUserId String
  acceptedAt       DateTime @default(now())
  evidence         Json?

  organisation Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  policy       CompliancePolicy @relation(fields: [policyId], references: [id], onDelete: Restrict)
  acceptedBy   User             @relation("PolicyAcceptanceAcceptedBy", fields: [acceptedByUserId], references: [id], onDelete: Restrict)

  @@unique([organisationId, policyId, acceptedByUserId])
  @@index([organisationId, acceptedAt])
  @@index([policyId])
}

model DataRequest {
  id                String            @id @default(cuid())
  organisationId    String
  requestedByUserId String
  subjectUserId     String?
  subjectEmail      String?
  type              DataRequestType
  status            DataRequestStatus @default(OPEN)
  reference         String            @unique
  notes             String?           @db.Text
  dueAt             DateTime
  completedByUserId String?
  completedAt       DateTime?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  requestedBy  User         @relation("DataRequestRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)
  subjectUser  User?        @relation("DataRequestSubjectUser", fields: [subjectUserId], references: [id], onDelete: SetNull)
  completedBy  User?        @relation("DataRequestCompletedBy", fields: [completedByUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, dueAt])
  @@index([subjectUserId])
}

model RetentionPolicy {
  id                  String    @id @default(cuid())
  organisationId      String    @unique
  rawPlaybackDays     Int       @default(395)
  playerHeartbeatDays Int       @default(90)
  audioProjectDays    Int       @default(730)
  supportTicketDays   Int       @default(730)
  auditDays           Int       @default(2555)
  reviewedByUserId    String?
  reviewedAt          DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  reviewedBy   User?        @relation("RetentionPolicyReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
}

model RetentionJob {
  id                String             @id @default(cuid())
  organisationId    String
  requestedByUserId String
  status            RetentionJobStatus @default(QUEUED)
  dryRun            Boolean            @default(true)
  policySnapshot    Json
  cutoffs           Json
  candidateCounts   Json?
  errorMessage      String?
  completedAt       DateTime?
  createdAt         DateTime           @default(now())

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  requestedBy  User         @relation("RetentionJobRequestedBy", fields: [requestedByUserId], references: [id], onDelete: Restrict)

  @@index([organisationId, createdAt])
  @@index([status, createdAt])
}

model SupportTicket {
  id                String                @id @default(cuid())
  organisationId    String?
  createdByUserId   String
  assignedToUserId  String?
  priority          SupportTicketPriority @default(NORMAL)
  status            SupportTicketStatus   @default(OPEN)
  reference         String                @unique
  subject           String
  description       String                @db.Text
  linkedEntityType  String?
  linkedEntityId    String?
  incidentStartedAt DateTime?
  resolvedAt        DateTime?
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  organisation Organisation? @relation(fields: [organisationId], references: [id], onDelete: SetNull)
  createdBy    User          @relation("SupportTicketCreatedBy", fields: [createdByUserId], references: [id], onDelete: Restrict)
  assignedTo   User?         @relation("SupportTicketAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, status, createdAt])
  @@index([assignedToUserId, status])
  @@index([linkedEntityType, linkedEntityId])
}

model AuditExportSeal {
  id               String   @id @default(cuid())
  sequence         Int      @unique @default(autoincrement())
  organisationId   String
  exportJobId      String   @unique
  previousSealHash String?
  contentSha256    String
  sealHash         String   @unique
  rowCount         Int
  fromAt           DateTime
  untilAt          DateTime
  createdAt        DateTime @default(now())

  organisation Organisation    @relation(fields: [organisationId], references: [id], onDelete: Restrict)
  exportJob    ReportExportJob @relation(fields: [exportJobId], references: [id], onDelete: Restrict)

  @@index([organisationId, sequence])
  @@index([organisationId, createdAt])
}

model AnalyticsHourlyAggregate {
  id                       String    @id @default(cuid())
  organisationId           String
  playerId                 String
  playerName               String
  locationId               String
  locationName             String
  zoneId                   String
  zoneName                 String
  bucketStart              DateTime
  plannedCount             Int       @default(0)
  campaignPlannedCount     Int       @default(0)
  schoolPlannedCount       Int       @default(0)
  playbackStartedCount     Int       @default(0)
  playbackCompletedCount   Int       @default(0)
  playbackFailedCount      Int       @default(0)
  playbackInterruptedCount Int       @default(0)
  musicCompletedCount      Int       @default(0)
  promoCompletedCount      Int       @default(0)
  schoolCompletedCount     Int       @default(0)
  heartbeatCount           Int       @default(0)
  firstHeartbeatAt         DateTime?
  lastHeartbeatAt          DateTime?
  createdAt                DateTime  @default(now())
  updatedAt                DateTime  @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, playerId, bucketStart])
  @@index([organisationId, bucketStart])
  @@index([organisationId, locationId, bucketStart])
  @@index([organisationId, zoneId, bucketStart])
}

model AnalyticsAggregationCursor {
  organisationId      String    @id
  lastIntentCreatedAt DateTime?
  lastIntentId        String?
  lastProofReceivedAt DateTime?
  lastProofEventId    String?
  updatedAt           DateTime  @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
}
